<%
' Copy this file to chatproxy.config.asp on the server, then replace the
' placeholder. The real file is ignored by Git and produces no HTTP output.
' chatproxy.asp includes this file server-side, so it must exist; the runtime
' key is read from the GROQ_API_KEY environment variable instead.
DeskTalkBigModelApiKey = "replace-with-your-groq-api-key"
%>
