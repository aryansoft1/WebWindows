# 任务同步台账 · OpenCode → Codex

> **本文件是 OpenCode 与 Codex 之间的任务交接台账。**
> Codex 额度恢复后，**先读本文件最新状态再继续处理**，不要重做已完成的步骤。
> 单次任务的详细交接记录仍按惯例另写 `docs/HANDOFF_<日期>_<主题>.md`，本文件只做总账。

---

## 0. 强制规则（双方共同遵守）

1. **断网续作**：任何任务遇到网络中断、请求超时、连接失败，**必须重连重试并从断点继续，不得退出任务**。
2. **全量同步**：OpenCode 执行的**所有任务**（开始、里程碑、阻塞、完成、移交）都必须同步写入本文件，让 Codex 随时可知。
3. **先读后写**：Codex 接手前先读本文件；继续完成后**必须回写状态**，保持台账闭环。
4. **不重做**：状态为 ✅ 的条目不要重做；状态为 📤 的条目由 Codex 继续；状态为 ⏸️ 的条目先看「下一步 / 阻塞原因」。

### 状态图例

| 标记 | 含义 |
|---|---|
| ⏳ | 待处理 |
| 🔄 | 进行中（OpenCode 执行中） |
| ⏸️ | 阻塞（写明原因，通常是 Codex 额度 / 等人工 / 等环境） |
| 📤 | 已移交 Codex，等额度恢复后继续 |
| ✅ | 已完成（保留记录，勿重做） |

---

## 1. 进行中 / 待处理任务

| ID | 任务 | 状态 | 已完成 | 下一步 / 阻塞原因 | 更新时间 | 执行方 |
|---|---|---|---|---|---|---|
| T-000 | 建立本同步台账并写入 AGENTS.md 规则 | ✅ | 台账 + 规则 9 已提交 | — | 2026-09-24 | OpenCode |
| T-009 | 问乡（问道 transit）Q2 同城查询修复并上线 `2026.09.24.2` | ✅ | 根因确诊（12306 为同城级查询，客户端按查询站名切片经停表→失败）；`api/railway-proxy.asp` 输出行内 fromCode/toCode/站名（新增 ParseNameMap）+ `transit-providers.js` 候选车次有界回落（≤5，全败保留真实错误码）；新增同城/404 回落/全败 3 场景防回归；commit `159ca1e` 推送、preflight 通过、8 文件上传回读全过（备份 marker `20260924-wendao-rail-schedule-fix`）、入口冒烟 176 deps、**线上 E2E Q1+Q2 全过（E2E_ALL_PASS）**；工作区恢复完成（子模块 10/10 == 基线；父 37 行 = 基线46 − 9 个已被 `33ef9e5` 吸收进仓库的文件，内容哈希 `efec9db` 工作区/stash/HEAD 三方一致，即早前"staged 文件谜团"实为 T-000 提交扫入索引）；终验 **86/86** | — | 2026-09-24 | OpenCode |
| T-010 | `2026.09.24.2` 同步上传独立复核（用户「同步后上传」请求收口） | ✅ | 确认并行任务已收口：commit `159ca1e` 推送、manifest/catalog `2026.09.24.2`、线上 `release-version.asp` 报 `2026.09.24.2`（prev `2026.09.24.1`）；独立复核全过：上版 7 文件备份哈希 == 上版 manifest（规则 5）、FTP 源码回读 3/3（`railway-proxy`/`release-version`/`mobile-version` `.asp`）== 本地 manifest、HTTP 静态回读一致、线上 manifest == 本地（`1cd544a13d30`）、preflight `already_deployed`、门禁三连过（清单 256、入口依赖 176、mobile 契约）、全量 **86/86**；`stash@{0}` 与 r3 临时 exclude 已由并行任务恢复/移除、启动项集成回工作区、启动项/visitor 冒烟转绿 | 启动项集成（settings/index/main/app-registry/settings.js/css，混有 login/dist/package 等无关 WIP）未提交未上线；线上 `settings.html`/`index.html` 与 HEAD 内容分叉见 T-006，是否提交+上线待用户决策 | 2026-09-24 | OpenCode |
| T-011 | 真实浏览器验证问乡并修复两个 Node E2E 无法发现的缺陷，上线 `2026.09.24.3` | ✅ | 用户现场截图指出「并没有好好测试」——上轮只验了库入口，未覆盖真实页面。真实浏览器复现后定位并修复：①`normalizeStopTimes` 跨日进位方向错误（把「同站到 00:53/发 00:57」误判跨零点，逐站 +86400，页面显示 `24:53/49:35/100:03`），改为仅在「与前站倒退」或「发车早于到达」时进位，真正跨零点车仍正确显示 `31:05`；②`road.html` 缓存戳未随 r4 的 JS 变更递增（仍 `?v=20260924-1`），致浏览器复用旧 JS、r4 修复对已访问用户不生效（截图症状：卡在 GTFS、`action=stations` 红色、无 leftTicket），本次递增 `?v=20260924-2`；新增 G4190 真实经停 + 同站跨零点回归测试；commit `f6e75ba` 推送、preflight 过、9 文件上传回读全过（`road.html` 在 manifest 前一位，规则 7）、入口冒烟 179、**真实浏览器 UI 端到端复现并验证**（成都东→西安北 G4190，5 站时刻 `00:53/01:35/02:27/03:15/04:30`，stations→GTFS→leftTicket→schedule 全 200、控制台零错误）、全量 86/86；工作区已还原（父 37/37、嵌套 10/10 == 基线，groq 哈希一致） | — | 2026-09-24 | OpenCode |
| T-012 | 修复「连点查询卡死在照会中」，上线 `2026.09.24.4` | ✅ | 用户第二次现场截图（Network 显示 `transit-proxy.asp?action=stops` 状态 **(canceled)**、无任何 `leftTicket` 请求、界面停在「GTFSの公共交通データを照会中…」）+ Console 证据确证：`searchJourneyWithFallback` 对**任意** `AbortError` 直接 rethrow、跳过 12306 回落。但 AbortError 有两义：①用户主动取消（外部 signal 已 aborted）应整体放弃；②GTFS 阶段内部中止（`fetchWithTimeout` 内部控制器 / 阶段预算 / 上游中断 / **连点时旧轮请求被取消**）必须回落。旧实现混淆二者 → onFallback 不触发、leftTicket 从不发出、界面永久停留。修复：GTFS 仅在 `signal?.aborted`（真·用户取消）时 rethrow，其余 AbortError 一律 onFallback + 继续 12306；12306 阶段非用户取消的 AbortError 转显式 `rail_failed`；缓存戳 `?v=20260924-3`；**修正了固化错误语义的旧测试断言**（原「GTFS 抛 AbortError 应中止」改为「应回落」）并新增用户取消/中止后新查询不受污染两场景；commit `066e8f7` 推送、preflight 过、9 文件上传回读全过、全量 86/86；**真实浏览器验证连点 3 次（4 个 transit-proxy 被取消）仍成功回落出 G4190，随后第 4/5/6 次查询（换日期 D1998、换城市 北京南→上海虹桥 G531 13 站）全部成功**；工作区已还原（父 37/37、嵌套 10/10） | — | 2026-09-24 | OpenCode |
| T-013 | 修复「查询被自身表单导航杀掉」，上线 `2026.09.24.5`（真正根因） | ✅ | 用户第三次现场截图给出决定性反证：**单次点击**、21.5 秒后仍卡在「照会中」，与点击次数无关——否定了 T-012 的判断（若仅是 AbortError 跳过回落，修复后应 8 秒内回落）。真实浏览器探针确证根因：`<form id="transit-search-form" method="get">` 的 submit 监听器直接绑 `searchJourney`、**未调用 `preventDefault()`**（探针实测 `defaultPrevented=false`, `form.method=get`）。后果链：点「交通を検索」→ JS 发起查询 → 浏览器同时执行原生 GET 提交 → 页面导航/刷新 → `beforeunload` 触发 → `state.controller.abort()` **把刚发起的查询自己杀掉** → 界面永远停在「照会中」。这也解释了「秒出现」：查询在导航瞬间即被中断，根本等不到阶段预算。修复：①submit 监听器改为 `event => { event.preventDefault(); searchJourney(); }` ②catch 中 AbortError 分支：若 `state.controller` 已被更新查询接手则静默返回，否则状态改回 `transitPrompt`、绝不永久停留「照会中」③`transit-app.js` 缓存戳递增 `?v=20260924-2`；新增回归断言锁定 submit 必须 preventDefault；commit `7f5b7a9` 推送、preflight 过、10 文件上传回读全过、全量 86/86；**真实浏览器用真实鼠标点击 submit 按钮验证：URL 完全未变（`?formfix=` 原样保留、无原生导航）、G4190 成都东 00:57→西安北 04:30 共 5 站、12306 三连 200**；工作区已还原（父 37/37、嵌套 10/10） | — | 2026-09-24 | OpenCode |
| T-014 | 修复「点其它车次无反应」+「地图不跟随」，上线 `2026.09.24.6 ~ .10` | ✅ | 用户报告两个问题，真实浏览器逐一复现并修到底：**(1) 候选车次点击无效**——`selectTrain` 传了 `trainNo`，`searchJourney` 根本没读 `overrides.trainNo`；补上后发现 `searchJourneyWithFallback` 的参数解构里也没有 `trainNo`、调用 `chinaRail.searchJourney` 时未透传，值在两处被丢弃（.6/.7 两次修复才补全链路）。**(2) 地图不跟随**——三层次根因：①Photon 地理编码全线 400（实测 `lang=zh`/`lang=ja`/`defaultlang=zh` 均 400，只有 `en`/`de`/`fr`/`default` 受支持，旧代码固定发 `lang=zh`）→ 经停站无坐标 → shape 空；②`updateMap` 的 `mapReady` 门禁在结果先于地图就绪时直接 return，fit 请求被丢弃（.8 加 `pendingFit` 挂起并在 load 后补执行）；③**MapLibre 的 `load` 与 `idle`/`isStyleLoaded` 在 OpenFreeMap 下长期为 false**（实测 `loaded()=false`、`isStyleLoaded()=false`、`getSource('transit-shape')=undefined`，但容器 1546×1246 正常、瓦片已渲染）→ `mapReady` 永不为真（.9/.10 改为「样式对象可用 + 容器有尺寸」判据）。真实浏览器终验：首次查询 G4190 5 站、地图中心由 139.7/35.68(z5) 平滑移至 **106.61/32.59(z7)**；点第 2 个候选 G4868 → 请求 `78000G486801`、路线时刻全更新（02:13→05:45、4 站）、地图跟随。commit `d95ceed`/`b1446f0`/`f1859af`/`58670e6`/`bb8c6f1` 均已推送，每轮 preflight + 上传回读全过；工作区已还原（父 37/37、嵌套 10/10） | — | 2026-09-24 | OpenCode |
| T-015 | 海外 GTFS 查询修复 + 错误文案，上线 `2026.09.24.11 ~ .13`；如实记录无法修复项 | ✅ | **海外根因**：`searchStops` 不做相关性排序（「名称包含即命中」），搜「東京」首条是北海道旭川的「東京農業大学」(144.23/43.97)、搜「名古屋」首条是四日市，代码盲取 `stops[0]` 必然拿错站 → `getDepartures` 返回 0 → 误报「无直达」。修复：新增 `rankStopCandidates`（名称匹配度 + 以最佳匹配组质心为参考剔除同名异地站 + 去重）、起点候选逐个尝试（≤4）、终点匹配任一候选、单趟 trip 失败只跳过该趟。**文案修复分两轮**：.12 只认 `gtfs_trip_unavailable`，但真实浏览器实测 `onFallback` 收到的是 `transit_timeout`（候选站×多趟 trip 累计耗时先触发 8s 阶段预算），.13 才把超时/上游码纳入并在「12306 报 station_not_found（站表无此站名＝海外线路）」时才显示上游故障，国内 `no_train` 等仍走覆盖范围提示。**真实浏览器终验**：東京→名古屋 显示「公共交通上游暂时无法提供班次详情」；成都东→西安北 不受影响（G4190、5 站、00:53→04:30、地图 106.54/32.52）。**如实记录无法从客户端修复项**：①12306 不提供过去日期（502 upstream_blocked），当天也只返回未发车车次，故「搜过去/运行中车次」是数据源限制；②Transitland 公共 API 现需认证（401），代理 trip 接口 500，且 `api/transit-proxy.asp` **未纳入版本管理**，无法从仓库侧修复上游；③运行中位置估算逻辑本身正常（实测 estimated 坐标 (106.031,32.400) 正确）。部署期间两次 curl exit 56 网络中断，按断点续跑，并从服务器备份恢复了被中断的 `auth-session.js`、`system-apps.json`，另发现并恢复 Earlier 轮次遗留缺失的 `api/mobile-version.asp`；最终 7/7 ASP（FTP 源字节）+ 10/10 静态文件回读全过 | — | 2026-09-24 | OpenCode |
| T-016 | 将服务器上未纳管的 transit/flight/navigation 等 ASP 纳入版本管理（并给 Codex 留档） | ✅ | 审计 `/wwwroot/api` 与仓库差集：服务器 29 个有效文件、仓库仅跟踪 20 个，**11 个长期在服务器运行却从未入库**（违反 AGENTS 规则 3）。已从 FTP 取回并 commit `62adaa7` 推送：`adsb-trace-proxy.asp`、`camera-login.asp`、`dt_discovery.asp`、`flight-proxy.asp`、`flight-proxy.config.example.asp`、`navigation-proxy.asp`、`navigation-proxy.config.example.asp`、`network-speed.ashx`、`storage-quota.asp`、`transit-proxy.asp`、`transit-proxy.config.example.asp`。**安全处理**：server 版 `transit-proxy.asp` 硬编码 Transitland Key（`Const WEBWINDOWS_TRANSITLAND_API_KEY = "iwa_live_..."`），已改为 `Server.Execute` 读 `transit-proxy.config.asp`（对齐既有 flight-proxy 模式）；`.gitignore` 新增 `api/*.config.asp` + `!api/*.config.example.asp`，使 `navigation-proxy.config.asp`（内含高德/百度/ORS 真实 Key）不再可能被误提交；入库前用 6 类规则扫描 11 个文件，**0 处疑似凭据**。**本次只入库、未部署**：部署新版 `transit-proxy.asp` 前必须在服务器建 `api/transit-proxy.config.asp`，否则 `action=stops/departures` 会 503 | **①T-015 遗留的海外 500 已定位真因**（不是 key 失效）：用真实 Key 实测——无 Key=401、带 Key 的 `stops`/`departures`/`routes/{k}/trips` 均 200，唯独单班次端点 `/routes/{k}/trips/{tid}` 一律 **500 `parameter error`**（穷举 include_geometry/include_alerts/include_stop_times/内部 id/feed 前缀共 10+ 变体全失败），列表端点又不返回 `stop_times` ⇒ **Transitland 上游已无法提供经停明细，海外直达查询需换源或等上游修复**。②12306 移动端 `getTrainMapLine`（走向）/ `travelServiceQrcodeTrainInfo`（运行信息）端点存在，但**从本机 IP 实测失败**（`操作失败` / `data:{}` / 403），需在生产服务器复测才能评估。③用户问「换源还是加源」的结论：**加源不换源**——12306 仍是余票/时刻权威源，只新增一层「仅 running 时调用」的定位增强（优先 12306 走向 polyline 提精度，其次第三方实时源带缓存/熔断，兜底现有时刻线性插值），免费额度只花在运行中查询，符合用户设想 | 2026-09-25 | OpenCode |

| T-017 | 国内「运行中车次 / 当前位置」可行性实测 + 数据源方案定案（换源 vs 加源） | ✅ 调研完成（实现待用户决策） | 用户授权在生产服务器实测 12306 移动端点。诊断脚本 `tools/diagnostics/probe-12306-mobile.asp`（commit `7716bd9` + 转义修正），**用后已从服务器删除、HTTP 404 确认**。**①服务器侧实测（iPhone 微信 UA + Referer）**：`getTrainMapLine`（车次走向/真实 polyline）200 但 `status:false, errorMsg:"操作失败"`；`travelServiceQrcodeTrainInfo`（运行信息）200 且 `data:{}`（紧凑/带横线两种日期都空）；`bigScreen/getTrainList`（公开资料里唯一能列「已发车+运行中」的端点）**403 openresty**（需小程序 token）；同域 `kyfw.../station_name.js` 200/136KB ⇒ 服务器能直连 12306，**失败原因是接口鉴权而非 IP/网络**。**②绕过本方代理直连 12306 `queryG`（先 `/otn/leftTicket/init` 取 cookie）**：`date=今天(2026-09-24 北京 23:12)` **只返回 2 趟**(23:25/23:40) ⇒ **12306 上游自己过滤掉当天已发车/运行中车次，我方代理无责**；`date=未来(2026-09-26)` 107 趟、按时刻粗判运行中 7 趟；变体 `queryA/queryZ/query/lcQuery` 全部 302，只有 `queryG` 可用。**③方案定案：加源，不换源**——12306 保留为时刻/余票权威源（唯一免费权威源，换掉会丢余票/停站并继承别家限制）；**零成本「当日全量快照」层**即可解决「发现运行中车次」：北京 00:05 之后查当天时 12306 只缺 00:00–00:05 的车，服务端把当天结果合并落盘（Application 对象/文件）后，全天都能筛出运行中车次，且经停实测对已结束/运行中/未发车都可用；位置沿用现有时刻插值（已实测 `estimated` 正确）。只有「真实 GPS / 实际晚点」才必须第三方：公开免费额度极小（六派 10 次/日、小渡聚合不稳定），需付费或自建聚合，且应按「仅 running 时调用 + 车次/日期缓存 + 熔断」隔离，不进主链路 | 待用户决策：是否实现「当日全量快照」层（改 `api/railway-proxy.asp` 落盘 + 客户端筛选项），以及是否需要付费实时位置源 | 2026-09-25 | OpenCode |

| T-018 | 实现「当日快照 + 运行中车次」并上线 `2026.09.25.1 ~ .3`（含两次真实浏览器抓出的真 bug） | ✅ | 用户选定「加源不换源」中的零成本方案后实现。**①代理「当日快照」**：`api/railway-proxy.asp` 新增 `includeElapsed=1`，把每次查到的车次按 `trainNo` 合并进 Application 内存（TTL 2 天、过期条目顺手清理），当天早些时候查到的车次即使已发车/已到达仍可返回；上游拒绝过去日期时若有快照则照常返回；响应加 `"snapshot":true`。所有 JSON 标记串一律 `Chr(34)` 构造——实测 VBScript 里四个连续引号会被解析成两段（`"""" & x & """:"` 静默少一个引号、marker 永远匹配不到），已加测试守卫；VBScript 无编译器，用 `cscript` 编译期校验（剥 `<% %>`、按 UTF-16LE+BOM 写出，否则中文注释被按 ANSI 读取产生假阳性）。**②客户端**：`beijingClock/railSeconds/railDayDifference/classifyRailTrain` 按中国标准时间判定 `running/past/upcoming`（跨零点车次 23:30→26:10 在次日 00:00 仍算运行中）；`pickTrains` 当天按「运行中→未发车→已通过」分组，过去日期放宽到 10 趟并按距所查时刻排序，未来日期保持原升序；`pickInitialCandidate` 查「此刻」默认选中运行中车次、查具体时刻取最接近的一趟（即「搜过去的车次」）；`getLeftTicket` 仅在查询日 ≤ 北京当天时带 `includeElapsed`；候选列表加「运行中/已通过」标签（复用既有 `serviceRunning/serviceBefore` 文案 + 新增 CSS）。**③真实浏览器连续抓出两个真 bug**：`.2` 修「缓存命中后 `trains:[]`」——`ComposeLeftTicketJson` 的 `rows` 声明为 `ByRef`，而实参是 `SnapshotMergeRows` **函数返回的数组**，VBScript 传参下退化为空，快照被写成空车次（同时加防御：合并结果为空则直接存本次响应）；`.3` 修「渲染中断」——问乡面板默认隐藏、容器尺寸 0 时 MapLibre transform 未初始化，带 `padding/duration` 的 `fitBounds` 抛 `Cannot read properties of undefined (reading 'lng')` 并冒泡中断整次渲染（逐步定位：无参 fitBounds/maxZoom 正常、padding/duration/easeTo/jumpTo 全抛、resize 后 fitBounds 恢复），改为 `fitBoundsSafely()` 先 `resize()` 再降级无参 fitBounds，`updateVehicle` 包 try/catch。**④部署事故与恢复**：FTP 链路本轮极不稳定（curl 28/56 反复），脚本「RNTO 备份→STOR」窗口内两次把 `api/railway-proxy.asp` 留在缺失状态（问乡中国铁路整体不可用），均从服务器备份 + 上一版 manifest 哈希对账后用 STOR 恢复；期间还把 `transit-providers.js`/`transit-app.js` 弄成 404；**我方就绪探测误用 curl 不存在的 `--list`（恒返回 2）导致长时间误判「FTP 不可用」**，改正后单文件逐个上传，11/11 全部回读通过。另把 manifest 的 uploadFiles 顺序改为「静态优先、railway-proxy.asp 靠近末尾」，降低中断时的爆炸半径。**终验**：线上 `2026.09.25.3`，真实浏览器（时钟前移 4.5h 模拟运行中）成都东→西安北 命中 D4730、状态「运行中」、位置「推定位置」、候选带「运行中」标签、状态栏无错误；同一区间连续两次查询均返回 106 趟（快照路径正常）。**遗留**：本自动化窗口内 MapLibre 相机子系统本身不稳定（同一 `fitBounds` 先成功后抛、`easeTo/jumpTo/setCenter` 均抛），地图跟随已在 `.10` 轮真实用户环境验证过（中心 139.7/35.68→106.61/32.59），需在正常可见的窗口复验一次 | 待人工：①在正常显示的窗口确认地图跟随；②`api/transit-proxy.asp` 的 Key 外置版仍**未部署**（等轮换）；③Transitland 单班次端点上游 500 需换源或等修复 | 2026-09-25 | OpenCode |

| T-019 | 修「运行中车次仍不出」+「海外仍不行」，上线 `2026.09.25.4 ~ .9` | ✅ | 用户反馈两点，**都不是语言问题**，都是可修的真 bug。**①海外 500 真因**：Transitland v2 单班次端点只认**内部数字 id**——`/routes/{rk}/trips/20B0809000` → 500 `parameter error`（无 stop_times），`/routes/{rk}/trips/12368625337` → 200 且 `stop_times=18`；departures 响应里两种 id 都有。改用 `departureTrip.id` + 代理 `IsTripRef` 放行两种形态。实测：修复前 10 个候选 0 个拿到经停，修复后 10/10；真实页面「東京→池袋」直达成功（9 经停、09:04→09:20、线路名取 `trip_short_name` 回落）。**②运行中车次仍不出**：①服务端快照只活于 Application 内存，应用池回收即丢（线上实测 10:31 查询无 `snapshot` 标记、只有 10:42 之后的 77 趟）；②尝试落盘时被两个**我自己的 API 误用**误导——`CreateTextFile` 不支持第 4 参（错 450）、`OpenTextFile(..., -65001)` 非法（错 5），改用 ADODB.Stream 后重新探测才得到真相：**该托管应用池对 `data/`、`cloud/file/`、`logs/` 完全没有写权限**（子目录与既有目录直写两种形态都 False），故服务端持久化不可行。改为**客户端 localStorage 按「日期+区间」累积**（`mergeRailSnapshots` 以 trainNo 去重合并，上限 400 条），并移除已证实不可用的落盘代码与 `__diag_snapshot_dir` 诊断 action。真实页面验证：真实查询填充 58 趟 → 时钟前移 5h → **5 趟「运行中」+ 3 趟「已到达」带标签排前**，默认选中运行中车次、位置「推定位置」。**③顺带修展示缺口**：`past` 状态补映射到 `arrived`（之前已通过车次无标签）、海外线路名回落 `trip_short_name`/`trip_headsign`（之前只显示「公共交通」占位）。**④transit-proxy.asp 真正纳管上线**：服务器已建 `api/transit-proxy.config.asp`（真实 Key 不入库、不在上传集），代理改为读该文件，线上不再有硬编码密钥；期间踩到 ASP 引擎**没有 `Dir()`**（报「未定义」）与 `Server.Execute` 变量不跨作用域（503），最终改为 FSO 判断存在性 + ADODB 读文件 + 正则取值。缓存戳 transit-providers/transit-app → 20260925-5。**残余限制（如实记录）**：若某趟车在当天第一次被查询之前就已发车、且此前无人查过该区间，则永远补不回来——彻底解决需引入不按当前时间过滤的第三方数据源（见 T-017 结论） | 待人工：①正常可见窗口复验地图跟随；②Transitland Key 轮换（已可随时改配置文件）；③`tests/system-dialog-smoke.mjs` 仍失败，源于用户 WIP 的 `app-registry.js` 加了 `?v=20260810.2` 而测试期望无版本参数 | 2026-09-25 | OpenCode |

| T-020 | 修「看不到运行中车辆位置」：Marker 创建顺序 + 相机不跟随，上线 `2026.09.25.11/.12` | ✅ | 用户反馈车次能查到了但**看不到车辆位置**。真实浏览器取证后是**两个独立缺陷**。**① Marker 创建顺序（.11）**：旧代码先 `addTo()` 再 `setLngLat()`，而 MapLibre 5.6 的 `addTo()` 结尾立刻用未定义的 `_lngLat` 调 `_update()` → `project(undefined)` 抛 `reading 'lng'`；此时元素已进容器、move/resize 监听已注册，留下「没有坐标却监听地图事件」的半残 Marker，地图一动就在渲染循环里抛错并**楔死渲染队列**（控制台刷 `Attempting to run(), but is already running.`），此后 jumpTo/setCenter/setZoom/fitBounds 全抛，地图不再跟随、标记永远停容器原点，每次查询再泄漏一个。证据：残留多个 `class="airport-marker maplibregl-marker"` 且 `style` 为空、矩形停在 (390,0)；同页新建干净地图一切正常（style/tile/glyph 实测均 200，排除样式/网络）。修法：`setLngLat()` 先于 `addTo()`、失败兜底摘除元素、新增 `purgeStrayVehicleMarkers()` 清理残骸、标记加 `transit-vehicle-marker` 专用 class + CSS 绝对定位。**② 相机动画不推进（.12）**：修完①后标记已定位，但相机仍停在默认 139.7/35.68、标记落在画布外 x=-297。实测 `cameraForBounds()` 一直能算出正确相机（106.535/32.525、zoom 7.45），但 `fitBounds({duration})`/`easeTo({duration})` **既不抛错也不动**，`isMoving()/isEasing()` 永久 true（窗口不可见/被遮挡时 rAF 不跑）；同步 `stop()+jumpTo()` 立即生效。修法：`focusBounds()` 改为可校验降级链（stop → cameraForBounds → jumpTo → easeTo → 带 options fitBounds → 无参 fitBounds），新增 `moveCameraVerified()` **每次都重新读取相机比对**（「没抛错但没动」判为失败），新增 `keepVehicleInView()`（切换候选时 `fit:false` 有意不重 fit，若车辆在视野外就带相机过去）。**真实页面终验**：相机跟到 (106.536/32.524)、`moving/easing=false`、点选运行中 D1661 后 `serviceState=running`/`positionStatus=estimated`/标记 `present+positioned`/`strays=0`/矩形在画布内、**MapLibre 报错 0**；标记落点反解 (108.0073, 31.9609) 距万源站约 35px（16:36 发车后 1 分钟），位置估算与时刻表一致。另：`getDebugState()` 新增 `vehicleMarker`/`camera` 字段，同类问题可直接观测。**并发事故**：期间发现另一会话（共享工作区、做 T-006）把我的 `.11` 清单先传上生产但只传了 3 个文件，导致线上清单声称的哈希对应未上线文件；已按规则 5 人工对账（核验线上字节 == 本地上传切片）后补齐 5 个文件，`.11`/`.12` 最终 8 文件 + 清单字节全部一致。**教训**：相机调用必须校验结果；我的 FTP 就绪探测一度漏带 netrc 而误报「FTP 不可用」跳过上传，已修正 | 待人工：①正常可见窗口复验地图跟随（本自动化窗口动画不推进属环境特性，但同步跳转已验证可用）；②Transitland Key 轮换；③`tests/system-dialog-smoke.mjs` 仍失败（用户 WIP 的 `?v=` 与测试期望不一致）；④**需协调并发部署**：多个会话共享同一工作区与生产环境，建议部署前加锁或分工作区 | 2026-09-25 | OpenCode |
| T-021 | T-006 合并内容 14 文件完整上线 `2026.09.25.13`（含 `.10` 并发交错与线上清单记录修复） | ✅ | 用户「相关功能上传了吗」收口：**内容全部上线且三方验证闭环**。**①切片 14 文件** = 9 个 T-006 合并内容（`settings.js`、`app-registry.js`、`main.js`、`settings.css`、`settings.html`、`device-api.js`、`login.js`、`SystemManager/index.html`、`index.html`）+ 5 个 onlineReleaseFiles（`sysinfo.html`、`sysinfo.js`、`release-version.asp`、`mobile-version.asp`、`system-apps.json`，冒烟 else 分支强制同发）。**②缺口补齐**：`b9f08ae`（settings.js 3 处语言 `setItem` + app-registry `?v=20260810.2` 戳，取 prod/d4d291b 权威）、`f80899f`（`system-dialog-smoke` 契约随权威更新：fetch stub 剥查询串、期望带戳 URL）——**T-019/T-020 待办③「冒烟仍失败」就此解除**（套件 86/86 含该文件）。**③规则 5 对账三轮**：v1（9 文件 vs 线上 `.3` 清单：8 严格 + `SystemManager/index.html` 人工豁免——线上实际 `DA7874`=恢复包快照、清单记录 `f472ac`=历史陈值、本分支 `acc62e`=T-003 安全栈按日期权威胜出）、v2（14 文件 vs `.9`：12 静态严格 + SystemManager 豁免 + 2 个 ASP 改用 FTP 源字节才对上——HTTP GET 拿到的是执行输出）、v3（14 文件 vs `.12`：5 严格 + 9 豁免，豁免条件=实际==本地==已提交 HEAD，身份链确凿）；备份存 `Temp\opencode\t006\prod-backup-*`。**④发布波折**：预研清单 `.5`（`f8ab744`）被并行 `.6` 取代 → 按线上 `.9` 重生成 `.10`（`e8002a6`），preflight、双冒烟、86/86 全绿、15/15 上传双回读全过；**但并行 T-020 的 `.11` 清单在准备期抓取的是 `.9`（observed 与我清单落地之间的 TOCTOU），在我 `.10` 落地后覆盖上线** → 我 9 文件的线上清单记录停在 `.9` 旧值（记录≠实际），**实际字节全部正确且==本地**（对方视角的对账与复盘见 T-020 行，互为对照）。**⑤记录修复 `.13`**：observed 线上 `.12` + `--files` 14 全量重算（`49b9754`）→ 规则 5（5 严格+9 豁免）→ preflight `preflight_ok=true` → 15/15 上传（STOR 临时名→回读→RNFR/RNTO 双改名→回读）→ **终验 14/14 三方一致（实际==本地==清单记录），9 个陈旧记录全部修正**；终门禁清单冒烟 260、入口冒烟 183、86/86。**⑥经验**：FTP STOR 550 根因=登录根≠站点根、路径需 `wwwroot/` 前缀（清单 `root:/wwwroot` 印证）；动态 ASP 对账必须 FTP 源字节；上传窗口内 `release-version.asp` 瞬时 `Release information is unavailable`=重命名窗口、自愈。**⑦窗口管理**：我方窗口 stash×3（父 baseline/groq + 子模块）+ exclude 15 行，关闭时只删我的连续块（他们的 r5 块已自行移除），**基线 20 行逐行==快照**；窗口期间双方互蹭干净状态（其 `.11`/`.12` preflight 正用我窗口）。线上终态：`2026.09.25.13`、`scope=t006-merge-content`、`upload=15`、release/catalog 均 `.13` | —（内容、记录、门禁全部闭环；后续发版从 `.13` observed 即继承正确记录） | 2026-09-25 | OpenCode |

| T-022 | 车辆标记图标重做设计（高铁/动车=火箭头、普通车=普通火车、车头恒朝目的地方向），上线 `2026.09.25.14` | ✅ | 用户要求：运行中车辆图标原来只是「圆框」，要重新设计；**高铁、动车用火箭头**，**普通 K 字头或海外普通火车用普通火车图标**，且**车头必须朝向目的地方向**。实现：①`VEHICLE_SVG` 两套内联 SVG（viewBox 32×32）——`bullet`：蓝色渐变圆徽章 + 白色流线尖鼻 + 深蓝风挡带 + 裙板轮位，字头 G/D/C（含城际 C）命中；`train`：石板灰圆徽章 + 方正车头 + 双风挡 + 黄色前照灯 + 受电弓，K/Z/T 与**所有海外线路**命中。旧实现是 `textContent="🚄"` 的 emoji 药丸，故观感是圆框；现改为纯 SVG，不依赖系统 emoji 字形。②`vehicleBearing()`：方向取「当前位置 → 下一站」(`state.described.currentSegment.to`，依次退到 `stops[nextIndex]`、终点站)，球面初始方位角 `atan2(sinΔλ·cosφ₂, cosφ₁·sinφ₂ − sinφ₁·cosφ₂·cosΔλ)`；位置本就是上一站与下一站间插值，故该方向既是真实行进方向也等于目的地方向。③旋转落在**内层** `.transit-vehicle-glyph`——MapLibre 独占 marker 元素的 `style.transform` 做投影定位，车头 rotate 必须在子元素上（与之前 fitBounds 抢 transform 同类教训）；每次渲染刷新形态与角度，形态切换仅在 `dataset.kind` 变化时重建 SVG。④图标用 `DOMParser` 解析模块内静态常量模板注入，不违反项目「禁止 innerHTML」规则。**线上真实页面验证**：G2206 → `kind=bullet`、SVG 8 节点、旋转 **54.7°**（东北，与成都东→西安北走向一致）、标记定位且在画布内；K386 → `kind=train`、旋转 **80°**（普速走老线，走向不同角度合理）、`present+positioned`、`strays=0`；MapLibre 报错 0。海外「未发车」班次按设计不显示车辆标记（位置不可用），已核对符合预期。缓存戳 transit-app.js?v=20260925-8、navigation.css?v=20260925-3。**环境说明**：本自动化浏览器窗口不可见（截图需可见窗口），这正是 MapLibre 动画不推进的根因（rAF 被节流），也导致无法截图；`.12` 起相机已改同步 `jumpTo` 不再依赖动画 | 待人工：①用户刷新页面确认两套图标观感，如需微调（配色/尺寸/车头造型）可继续改；②正常可见窗口复验地图跟随动画观感；③当天首次查询前已发车的车次仍需第三方源；④Transitland Key 轮换；⑤`tests/system-dialog-smoke.mjs` 仍失败（WIP 的 `?v=` 与测试期望不一致）；⑥并发部署需加锁或分工作区 | 2026-09-25 | OpenCode |

| T-023 | 修海外「有数据却报错」：trip 详情按线路去重 + 如实报「无直达」（四语），上线 `2026.09.25.15~.17` | ✅ | 用户截图反馈：海外似乎有数据，但报错、不显示线路（入力 東京→名古屋）。**取证**：①直调 provider 抛的其实是**正确**错误 `direct_trip_not_found`，但耗时 **8.58 秒**；②网络面板 11 次 `action=trip` **全部来自同一线路同一 stop_pattern**（丸ノ内線），Node 复测 11 次全 200、各 23~25 个经停（**不是上游故障、也没有限流**）；③阶段预算 `gtfsStage=8000` 被撞穿 → 判 `transit_timeout` → 回落 12306 → 海外站名不在中国铁路站表 → `coveredErrorMessage` 命中「上游故障」分支，于是界面显示「公共交通上游暂时无法提供班次详情」。**数据事实**：已接入 feed 只有东京地铁 + 若干地方巴士，「東京」站 40 班全属丸ノ内線，名古屋候选站分散在名铁巴士中心（四日市）、富崎（名古屋）、一色大桥（桑名），**确实没有**东京→名古屋直达。**修复（.15）**：`diversifyTripCandidates()` 按 (route, stop_pattern) 去重 + 跨线路轮转（每线 ≤2 pattern、总数 ≤8），起点发车扫描 12→30 班；`gtfsStage` 8000→14000；新增 `tripDetailOk`（至少成功解析一趟即报无直达）；无直达文案列出已检查线路。**修复（.16）**：`onFallback(code, message)` 把 GTFS 侧文案带回（此前只传 code，界面显示的是 12306 的「未在站表中找到该站名」）。**修复（.17）**：provider 改为挂 `error.details.routes` 结构化详情、中文 message 仅供日志，**用户可见文案由 app 按当前语言组装**——新增四语 `errGtfsNoDirect`（zh/tw/en/jp），缺 key 时退回 `errNotCovered`。**线上真实页面终验**：東京→名古屋 trip 请求 **11→2**、耗时 **8.6s→3.0s**（provider 直调 2.26s）；四语文案全部正确（日「東京から名古屋への直通路線は…（確認した路線：丸ノ内線…）」、英「No direct route from 東京 to 名古屋 in the connected data sources (checked: 丸ノ内線…）」、繁/简同理）；对照組 東京→池袋（有直达）仍正常：9 个经停、相机 zoom 11。缓存戳 navigation-app.js?v=20260925-1、transit-providers.js?v=20260925-8、transit-app.js?v=20260925-11 | 待人工：①如需覆盖东京→名古屋这类跨城区间，要新增日本高速巴士/铁路 GTFS feed（现有 feed 无覆盖）；②用户刷新后确认海外提示文案与车辆图标观感；③正常可见窗口复验地图跟随动画；④当天首次查询前已发车的车次仍需第三方源；⑤Transitland Key 轮换；⑥`tests/system-dialog-smoke.mjs` 仍失败（WIP 的 `?v=` 与测试期望不一致）；⑦并发部署需加锁或分工作区 | 2026-09-25 | OpenCode |

| T-030 | T-006 日期权威回补：35 个线上落后文件上线 + 并发连环撞号后的记录修复，**上线 `2026.09.25.20/.22`**（本行两次撞号 T-024→T-025、对方又占 T-025，改号 **T-030** 避让对方递增序列） | ✅ | 起因：用户「线上没有你的内容」。**核验结论**：我方 `.13` 切片确在上线——apex `y0.hk`+`www.y0.hk` 双域名 `index/settings` 字节==本地、真浏览器渲染 `?v=20260923-startup-1`/`auth-session.js`/`data-boot-status` 全在、无 Service Worker 无 CDN（响应头仅 ETag）；用户看到旧版 = **常驻桌面壳标签页未刷新**（服务端不发 Cache-Control，旧标签不重新拉取），强刷即见。**全量 260 文件比对**（原始字节 + CRLF→LF 归一化 + git blob 首现判期）：113 一致、91 仅换行（无需处理）、2 docs 线上 404（`FILE_DIALOG_API.md`/`SYSTEM_DIALOG_API.md`，维持 T-006「docs 无需上线」判定，但其在 requiredFiles 属清单瑕疵）、**54 真差异 = 40 本地新 + 14 线上新**。40 本地新 = T-006 判「37 个本分支已最新（**保留**）」**从未下发上线**（`.13` 切片按任务定义只含 9 个合并产物），扣除并行在途 5（transit×2/road/system-apps/manifest）→ **净 35 回补**：`admin_api/{adminAuth,deleteDatacenter,saveDatacenter,visitorAnalytics}.asp`、`api/function-catalog.asp`、`cloud/browser/{search.asp,file-search.css}`、`aplay/contact/japan/software/web.html`、`SystemManager/{assets/js×5,datacenter,developer-platform,functions,users}.html`、`assets/js×{connectivity,contextmenu-guard,desktalk,desktop-functions,file-search,function-center,function-sync,locale-region,start-menu-functions,system-session,taskbar-experience}.js`、`assets/css/context_menu.css`、`worker_{SheetCreater,WriteEditor}.html`。**14 线上新不动**（9 asp 服务端配置 + `desktalk.css/ai-tool-registry.js/resource-open.js/worker_SlideEditor.html`，应反向合并进分支）。**方案**：`.20` = observed 线上 `.19` + `--files`（35 + 5 onlineRelease）+ scope `t006-date-authority-backfill`，完整门禁（规则 5 备份 vs `.19` → preflight → STOR 临时名+双改名双回读 → 清单冒烟 + 入口冒烟 + 全量 86/86）。**并发协调（撞号事故实录）**：本行 14:23 登记占位 `.20` 后 14:25 preflight 过、14:26 规则 5 全严格、14:26~14:34 上传 41/41 全回读——**但并行未读到本行，14:34:14 同样提交了他们的 `.20`（`cc14fc2`，scope `wendao-gtfs-multi-origin-candidate`，8 文件）并与其上传重叠**：双方内容字节均正确落地（重叠 4 文件源字节相同；线上现存他们清单 + 我方 41 文件字节），**但他们 observed 的是线上 `.19`，其清单中我方 41 文件记录停在旧值（实际==本地==我方 `e1629e1` 清单记录，身份链确凿）**；另双方台账行同号 T-024，本行改号 **T-025**（对方 ✅ 行保留 T-024 以免与其提交信息脱钩）。**撞号续报**：14:41 我方 `.21` 修复上传至 24/41 被 FTP `226` 瞬断（前 23 个为幂等同字节重传、回读全过、无损伤、无 `.__new` 悬挂；根目录/SystemManager 残留的 `.__bak` 为历次成功上传保留的旧版备份，运行时无引用，列为保洁项）；**14:43 并行再次同号抢发 `.21`**（`8f3c9f8`，线上 `.21` = 他们，且其清单覆盖本地、我方修复清单退守 `2a24cab`）。**内容侧已闭环**：我方 41 文件字节 14:34 起全部在线（其 `.21` 的 9 文件与我方 35 内容无交集，实测 `aplay/desktalk` 等 == HEAD）；唯一残项 = 其清单中我方 41 记录停在 `.19` 旧值（实际==本地==`e1629e1`/`2a24cab` 记录，身份链确凿）。**收尾 `.22` 记录修复 ✅（14:53~15:15 全链）**：占位 `7693063` 推送 → 14:57 开窗（父仓 baseline/子模块/groq 三 stash + exclude 20 项）→ preflight `release=.22/production=.21/upload=41` 过 → 规则 5 对账 **5 严格 + 35 身份链豁免 + 0 停止**（v6 备份）→ 首传 12/41 遭 FTP `System error.` 瞬断（服务端瞬态，与 14:41 `226` 同类、无竞态无损伤）→ 15:11 幂等重传 **41/41 全回读、manifest 最后 `UPLOAD_DONE .22`** → 15:12 终验：线上 rel/manifest/catalog **三处 `.22`**、41 文件**三方一致**（manifest 自身字节==本地、自记 hash 为空属设计）、双冒烟 2/2、发版前全量 **86/86** → 15:15 关窗（三 stash 全弹回 + exclude 还原，工作区回到基线脏集）。**最终差异账（260 基准）：40 本地新缺口归零**（35 我方 + 5 并行方 == HEAD），91 仅换行 / 2 docs 404 / 14 线上新维持原判 | 待办：①14 个线上新反向合并进分支（另立任务，9 个 asp 需甄别服务端配置）；②requiredFiles 含 2 个未上线 docs 的清单瑕疵；③91 个换行差异随文件重写自然消解；④prod 残留历次上传 `.__bak` 旧版备份（运行时无引用）待保洁；⑤**并发部署加锁/分工作区**——本轮实锤 4 次（版本号 `.20/.21` 各双撞 + 台账 T-024/T-025 各重号），台账占位对并行不构成约束，须物理加锁；⑥服务端无 Cache-Control、常驻标签页不自动刷新（本次用户抱怨根源之一），建议入口版本戳/提示刷新 | 2026-09-25 | OpenCode |

| T-024 | 车型图标二次修订：海外轨道=普通火车、巴士=大巴（并修 route_type 取不到），上线 `2026.09.25.18/.19` | ✅ | 用户反馈：海外查询的图标「好像是普通地铁，而不是普通火车、大巴（注意是大巴不是公交）」。**根因**：用户可见的车型标识有两处且都是 emoji——①结果标题 `LIB.routeIcon(route_type)` → 🚋/🚌/🚇（地铁/公交观感），且 **700~799（GTFS 扩展巴士类型）被错映射成 🚲 自行车**；②地图标记虽已是 SVG，但只有 bullet/train 两种形态且**海外一律按普通火车**。**修复（.18）**：新增第三形态 `coach`（大巴：长圆角车体 + 侧窗带 + 斜前风挡 + 行李舱线 + 后视镜 + 车轮，琥珀色徽章）；新增 `gtfsVehicleKind(route_type)`——巴士类 3/200~209/700~717/800 → coach，轨道类 0,1,2,5,6,7/100~117/400~405/900~906/1600~1607 → train（**地铁也用普通火车**，按要求不要地铁观感）；重画 `train` 为圆顶车体 + 车钩排障器，**刻意不画受电弓**（方盒车体加受电弓正是地铁观感）；结果标题改用与地图标记同一套 SVG（新增 `.transit-route-glyph` 20px），删除已无人调用的 `vehicleIcon()`；`routeIcon()` 的 700~799 由 🚲 改 🚌。**修复（.19）**：真实页面验证时发现 `journey.route.type` 仍为 null——Transitland 的 `/routes/{key}/trips/{id}` 响应里 **route 是空对象**（实测 keys=[]），route_type/route_short_name 只在 departures 列表里，于是海外巴士被当成轨道。改为构造 journey 时合并两处 route 元数据（`routeMeta = { ...listRoute, ...detailRoute }`），routeId/operator/route.* 全部改读 routeMeta。**线上真实页面终验**：四日市 62 路（`市役所前（四日市）→三本松（四日市）`，route_type=3、36 站）→ `headerKind=coach`、运营方「三重交通(四日市営業所)」、相机 zoom 11；東京→池袋（丸ノ内線）→ `headerKind=train`、运营方「東京メトロ」（routeMeta 合并后也正确了）；国内 D166 → `headerKind=bullet` + `markerKind=bullet`、车头旋转 1.5°、运行中/推定位置。缓存戳 navigation.css?v=20260925-4、transit-providers.js?v=20260925-10、transit-app.js?v=20260925-12 | 待人工：①用户刷新后确认三种图标观感（高铁火箭头/普通火车/大巴）与车头朝向；②查询失败时地图仍残留上次结果（绿线与圆点），是否要清除或提示；③如需覆盖东京→名古屋等跨城区间，需新增日本铁路/高速巴士 GTFS feed；④当天首次查询前已发车的车次仍需第三方源；⑤Transitland Key 轮换；⑥`tests/system-dialog-smoke.mjs` 仍失败（WIP 的 `?v=` 与测试期望不一致）；⑦并发部署需加锁或分工作区 | 2026-09-25 | OpenCode |

| T-025 | 海外支持「长途/跨运营商」线路：多候选并行 + 代理缓存；并给出新干线的数据源结论，上线 `2026.09.25.20/.21` | ✅ | 用户明确诉求：「我想要查询的海外是**新干线、樱花巴士**这样的线路，你查的都是公交车，所以你查不到」。**代码根因（.20）**：起点候选循环里只要某候选站「有发车」就 break，而「東京」「大分」这类站名被多个运营商/交通方式的 feed 同时收录，市内公交/地铁的站排在最前且必有发车 → 高速巴士站、铁路站这些真正可能到终点的候选**一次都轮不到**。改为逐个尝试全部候选站，并新增 `roundRobinTripCandidates()` 把 trip 详情预算**跨站轮转**分配。**回归与修复（.21）**：多候选后 `departures` 变成串行多次，而 Transitland `/stops/{key}/departures` 极慢（实测单次 1.7/5.3/5.7/10s 超时，4 次累加 22.7s），整次查询 29s 撞穿 `gtfsStage` → 界面误报「上游故障」（同一 provider 调用不带预算是成功的）。改为 `Promise.all` **并行**请求；`GTFS_TRIP_CANDIDATE_LIMIT` 8→6；`gtfsStage` 14000→20000；代理 `api/transit-proxy.asp` 新增时刻表响应缓存（Application 内存、TTL 600s、键由 URL 派生，**GTFS-Realtime 不缓存**，过期用 `Application.Remove` 清理并修掉旧代码里 `Application.UnLock key` 的无效写法）。**数据源结论（已取证，非代码问题）**：用 Transitland API 枚举 feed 确认——①**新干线数据源里没有**（`新大阪` 搜到 0 个候选；唯一带 JR 的只有 `f-chugoku~jr~jp`（JR 中国地区线）与 JR 四国/东日本巴士 feed），要支持必须新增数据源（NAVITIME / Yahoo! Japan Transit / 自建 JR GTFS）；②**高速巴士有**（亀の井、大分、大分交通、日本中央、日田、北海道拓殖、九州産交等）；③**樱花（さくら）巴士有 4 个社区巴士 feed**（葛飾区、荒川区、さいたま市桜区、西宮市）。**线上真实页面终验**：東京→池袋（地铁）恢复成功（`train` 图标、東京メトロ、9 站）；要町（大分駅）→ 桜町バスターミナル（**大分↔熊本跨县高速巴士**，21 站、九州産交バス、`coach` 图标、相机跟随）可查——正是旧逻辑漏掉的那类线路；反向不通时提示会列出跨 4 个 feed 的线路名；東京→新大阪如实报「未在已接入的数据源中找到直达班次」。缓存戳 transit-providers.js?v=20260925-12 | 待人工：①**新干线需决策数据源**（NAVITIME/Yahoo!/自建 GTFS），这是当前唯一无法靠代码解决的缺口；②「樱花巴士」请确认是哪一家（现有 4 个均为社区巴士）；③查询失败时地图仍残留上次结果，是否清除或提示；④当天首次查询前已发车的车次仍需第三方源；⑤Transitland Key 轮换；⑥`tests/system-dialog-smoke.mjs` 仍失败（WIP 的 `?v=` 与测试期望不一致）；⑦**并发部署仍需加锁**（本轮又出现他方先传 .20 清单、导致半部署需人工补齐） | 2026-09-25 | OpenCode |
| T-032 | 全球长途 provider 第三层降级（Rome2Rio 插槽，Key 未配置时完全惰性），上线 `2026.09.25.23/.24` | ✅ | 用户选定 B 方案（申请 Key + 并行做 A）。**服务端** `api/longdistance-proxy.asp`：同源代理，Key 只在服务器 `longdistance.config.asp`（`.gitignore` 已覆盖，模板 `longdistance.config.example.asp` 入库但**不入上传集**）；动作 `capabilities`（无 Key 可调，返回 `enabled` 布尔）/ `search`（白名单参数转发）/ `selftest`（**拿到 Key 后的第一步**：固定查询 東京→大阪，回摘要判断新干线是否覆盖、字段形状是否符合解析假设，**不回传 Key**）；上游地址锁死 `https://rome2rio.com`、Application 内存缓存 TTL 300s（缓存键**先剔除 `key=`**）。**客户端** `LongDistanceTransitProvider`：`capabilities()` 每会话探一次且**不绑查询 signal**（否则用户点取消会把 enabled:false 永久缓存进本次会话）；`normalizeLongDistanceRoutes()` 只收**单段**铁路/巴士（route_type 2/3 → 普通火车/大巴图标），轮渡/航空/自驾/换乘一律排除且**不猜未知 travelMode**（fail-closed）；ISO 时刻转「服务地墙上时间 + serviceDate + serviceTimezoneOffsetMinutes」（不做 Date 换算），由 `stopTimes` 站坐标拼 `shape`（[lng,lat]、去连续重复点），起终点缺坐标的方案直接排除。**降级链** GTFS(免费) → 12306(免费) → 长途(付费)，门禁：未配置 Key → 整段跳过；12306 回 `no_train/presale/schedule_failed`（站名在中国站表内）→ **绝不花钱**；只有 `station_not_found` 才调付费源；用户取消立即上抛。四语新增 5 个文案，`STATUS_KEYS` 补 9 个错误码。缓存戳 providers/app → `20260925-13`、navigation-app → `20260925-2`。**上线事故（.23 首次上传即 500，已在 .24 热修）**：漏写 `<%@ Language=VBScript CodePage=65001 %>` —— .asp 源文件是 UTF-8 无 BOM，IIS 按系统 ANSI（本机 GB2312/936）读取，中文多字节被拆坏 → 编译器报「未结束的字符串常量」，**编译期检查（cscript+UTF-16LE）发现不了，只有真机暴露**；已补首行声明 + `Response.CodePage`/`CharSet`，并新增断言守住。**真实浏览器终验**：能力探测 200/`enabled:false`、search 503 `not_configured`（中文正常，证明 CodePage 修复生效）、非法 action 400；国内查询 成都东→西安北 **付费源一次都没被碰**（连探测都没有）；海外查询 東京→新大阪 只发 1 次**免费探测**、0 次付费调用，提示文案与上线前逐字一致；第二次海外查询 `ldProbes:0`（探测已按会话缓存）；对照組 東京→池袋 仍正常出结果（GTFS 路径无回归）；控制台无 JS 错误。11 文件 + 7 文件两轮上传均「规则 5 对账 9/9 与 6/6 严格 → preflight_ok → 逐个回读 → 线上源字节 == 本地清单」，`2026.09.25.24` 终态 | 待人工：①**用户去申请 Rome2Rio Key**（Paid / no trial），拿到后放 `api/longdistance.config.asp` 的 `longDistanceApiKey`，先跑 `?action=selftest` 确认新干线覆盖与响应形状，再开 search；②`travelMode` 数字编码表未经官方文档逐项确认（apis.io 只有概览），selftest 的 `head` 片段回来后要按真实值校正 `LONGDISTANCE_MODE_CODES`；③轮渡/换乘暂不支持（无图标 / 渲染模型是单条 shape）；④查询失败时地图仍残留上次结果；⑤Transitland Key 轮换；⑥**并发部署仍需加锁**（本轮改用独立干净 worktree 部署，绕开了另一会话的 WIP） | 2026-09-25 | OpenCode |
| T-031 | 设置窗口「应用」区块：线上功能目录 500 根因修复 + 应用选项卡四语补全，上线 `2026.09.25.25` | ✅ | 用户诉求（范围收窄后）：「我所说的是设置窗口的应用那一块，其它的我单独处理。现在主要的问题是**线上没有设置中的应用** 以及**应用选项卡在其它语言中没有正确显示**」。**根因①**：线上 `api/function-catalog.asp` 恒 **500**（ASP 0126 找不到文件）——`e66c69e`（08-31）把 trust schema 外置为 `<!--#include file="../inc/trust-schema.asp"-->`，但该文件**从未进 requiredFiles、从未上传**（线上实测 404，`inc/conn.asp` 200），导致功能仓库接口只能靠前端容灾 JSON 兜底（`repositoryStatus.source=fallback`、控制台常驻「服务器功能仓库暂不可用」）。**核验**：prod `settings.html` 有 `appsTab` ✓；清空 localStorage+IndexedDB 的干净实测在壳内渲染 **17 个应用** ✓ → 用户「看不到应用」= 常驻壳/iframe 启发式缓存（同 T-030 结论），字节上线后需强刷。**根因②**：`assets/js/tw.js` 语言目录**缺应用选项卡全部键**（`应用`/`启动项`/两段整句说明/`启动项服务尚未就绪…`等 4 条状态/`后台启动`/`最小化启动`/`正常窗口`/`启用`/`毫秒`/4 条动态模板），jp/tw 下只能按词典做半截替换（线上实测「管理 WebWindows 应用的**デスクトップ**行为。启动项只保存**当前ユーザー**的配置」「启动项服务尚未**準備完了**」、h3 与 nav 的「应用」保持中文），en 有混语守卫则整段回退中文。**修复**：tw.js 三语各补 18 键（en 含 4 条动态模板为整句英文回填）；`tests/language-coverage-smoke.mjs` critical 清单从 18 → 36（缺键即测试失败）；`settings.html`+`index.html` 的 `tw.js?v=` bump 至 `20260925-settings-apps-i18n-1`（否则客户端命中旧字典缓存，修复不可见）；`inc/trust-schema.asp` 纳入 requiredFiles+上传。**方案**：`.25` = observed 线上 `.24`（并行连切 `.23`（Rome2Rio）/`.24`（CodePage 热修），避让后取号）+ `--files` 9 文件 + scope `t031-settings-apps-i18n`；regen 后全量 **86/86**。 **部署实录（全部完成）**：并行连切 `.23`(Rome2Rio)/`.24`(CodePage 热修) → 本方取 `.25`（T-031 占位行被并行 `6e582f1` 顺带提交入库）；regen（observed=线上 `.24`）→ 全量 86/86 → `84f903b` commit+push → 门禁（info/exclude 快照+隐藏未跟踪、基线 5 文件 stash、子模块 `diff.ignoreSubmodules=dirty`，全部已还原、stash 清零）→ **preflight_ok**（.25/.24、262 files、10 upload）→ 规则 5：8 文件 7 MATCH + trust-schema 新增(404) + **tw.js 人工对账放行**（线上字节 = git `276f5a09` 确切 blob（09-23）、清单记录 `4edbf171` 不在任何 git 版本 = **记录侧漂移**；`276f5a09` 是 HEAD 祖先、本地 = 祖先后继 + 本次新增键 → 无内容丢失，上传即修复记录）→ 上传首跑在**新文件** `inc/trust-schema.asp` 的 RNFR(旧→bak) 550 中断（脚本假定旧文件存在；`.__new` 已上传已回读）→ 手动 RNFR 补完（902B 字节一致）→ 重跑幂等 **10/10 双回读、manifest 最后** → 验证：release/catalog/scope=`.25/t031-settings-apps-i18n`、`inc/trust-schema.asp`=200、`function-catalog.asp` **500(ASP 0126 找不到文件) → 503(`trust-schema-required` 结构化 JSON)**、双冒烟 2/2、真实浏览器：**jp** nav「アプリ」+ 两段整句日语零混排 + 行控件「有効/バックグラウンド起動/最小化起動/通常ウィンドウ/ミリ秒」+ 17 应用、**en**「Apps/Startup/整句英文」、切回 jp 正常、registry=fallback（见遗留②）、线上 `settings.html` 已带 `appsTab` + `tw.js?v=20260925-settings-apps-i18n-1` | 遗留：①用户**强刷常驻壳**后确认（字节已上线，旧壳标签页仍缓存，同 T-030 结论）；②prod DB 需人工执行 `database/migrations/001_webwindows_trust_schema.sql`（超出 FTP 范围、需批准）→ function-catalog 才能回 server 源（当前容灾 JSON 兜底、17 应用不受影响）；③`upload-t006.ps1` 对「线上不存在的新文件」RNFR 550 需加容错；④其它页面半截翻译与天气等 i18n 按用户要求单独处理 | 2026-09-25 | OpenCode |
| T-033 | 全站响应补 `Cache-Control: no-cache`：修复「纯 URL 打开是旧壳」的结构性根因（`web.config`，清单外配置变更） | ✅ | 用户反馈分叉：`https://www.y0.hk/?i18nprobe=t031` **有**设置应用、纯 `https://www.y0.hk` **没有**。**诊断**：代码里 `i18nprobe` 零引用（惰性参数）；服务器对 HTML 只发 ETag+Last-Modified、**无 Cache-Control** → 浏览器按启发式「Last-Modified 的 10%」自算新鲜期（可达数天）→ 纯 URL 命中未过期旧缓存条目**根本不回源**（带参数 URL 必然回源）；我方自动化浏览器纯 URL 全新下载（transferSize 11397）+ 真实 `openWindow('settings')` 路径九导航项全绿 → **线上字节与流程无问题，分叉在用户浏览器的旧缓存条目**（另注：常驻已开标签页不导航不刷新者任何服务端修复都覆盖不到，同 T-030 结论）。**方案选型**：①URL Rewrite 出站规则（`RESPONSE_Cache_Control`+`pattern="^$"` 官方配方、只加缺失头不碰已有）最优，但 RETR 发现 **prod `web.config` 只有 21 行、与仓库 64 行版完全不同**——rewrite/HSTS/CSP 安全规则**从未部署**（属 T-003「必须一次性整体上线」，不能拆开塞）、URL Rewrite 模块 prod 是否安装未知；②`<location>` 枚举 ~34 个 html 冗长且漏新页；③**定案核心 IIS `customHeaders` 全局一行**（零模块依赖、prod 最小改动、覆盖未来新页，代价=回访重验证 ~71 静态资源 304 往返，HTTP/1.1 可接受）。**执行**：RETR prod 原件 1183B（含真实 DB 凭据——**绝不上传仓库占位版**；明文一度出现在 diff 输出中，记入轮换关注）→ 合成 +7 行 +340B（断言：XML 良构、凭据 2 行逐字节不动、原 21 行全保留、无 BOM、CRLF）→ 仓库侧 `customHeaders` 同步加行 + `build-production-rehearsal-manifest` 重生成钉值（3303→3534，生成器仅动 web.config 一项、无其它漂移）→ 全量 86：85 过，`wendao-transit-smoke` **stash 复证纯净 HEAD 同样失败**（Transitland 实况只回 metro-only 触发 out_of_scope_service，T-025 领域、与本改无关，首轮曾绿）→ `500dac4` commit+push（规则1 fetch+ancestor 复查过）→ FTP 原子改名上传（`.__new` STOR→回读一致→RNFR 翻转→**健康门一次过** root/api 200→**含凭据备份 `.__old` 即刻删除**：`.config` 隐藏规则罩不住 `.__bak/.__old` 扩展名，防公开下载泄露；回滚材料=本地原件+sha `4D38…89C5`；脚本内置失败自动回滚）。**验证**：`/`、`/index.html`、`/settings.html`、`tw.js`、`SystemManager/index.html`、`login/function-center/sysinfo` 全 200 `no-cache`；**304 响应也带 `no-cache`**（老缓存条目下次重验证即自动升级）；`release-version.asp` 双头叠加（应用层 no-store… + IIS 层 no-cache）取并集仍最严格、行为不变；`function-catalog.asp` 503= T-031 已知 DB 前置非回归；浏览器真实导航 reload=304 重验证、页面上下文 `fetch` 见 `no-cache`、设置窗口 jp 九导航「アプリ」+整句日语+应用清单正常。preflight 被既有基线脏集挡（非本改动；本变更为清单外单文件配置，以 备份+回读+健康门+自动回滚 替代）；入口依赖冒烟 183 依赖 ✓ | 遗留：①**用户对常驻壳 Ctrl+F5 强刷一次**——旧条目存的响应无 Cache-Control，启发式新鲜期内仍不回源，强刷（或自然过期）后升级为 no-cache、此后每次导航必重验证；②prod 与仓库 `web.config` 结构性分叉（rewrite/HSTS/CSP = T-003 待整体上线；凭据只在 prod）——两边均已各自加 no-cache 行，后续 T-003 上线时需合并保留；③全局 no-cache 的回访 304 开销如嫌重，可换 `<location>` 枚举 html-only；④MySQL 明文密码曾在对话中出现（与 T-008 FTP 同类），建议轮换；⑤`wendao-transit-smoke` 实况数据失败待 T-025/并行处理 | 2026-09-25 | OpenCode |

| T-034 | 海外只显示城际/干线铁路、高铁与长途大巴（排除地铁/捷运/通勤/市内公交），上线 `2026.09.25.26/.27/.28` | ✅ | 用户第三次明确定调并附截图纠正：**「海外接口注重的是普通铁路（不是地下铁、捷运、subway、台铁等这样类型）、高铁（如新干线、磁悬浮）」**；并明确两项取舍：长途大巴**保留**、市区通勤铁路**只留城际/干线**。此前把「東京→池袋（丸ノ内線/东京メトロ）」当验收对照组是**错的**，那条正是要排除的类型。**先取证再写判据**（不照抄 GTFS 规格书）：实测 丸ノ内線=type1/东京メトロ、有楽町線=type1、越後交通長岡線=type3，而用户要的大分↔熊本高速巴士**也是 type3** ⇒ 光看 route_type 分不出长途/市内，必须三重判据（制式 + 线路名 + 行程尺度）。**实现**：新增 `classifyOverseasService()`（导出，Rome2Rio 归一化共用），判定顺序「高铁名 → 巴士type(+尺度) → 城市制式type/名 → 通勤type/名 → 轨道type(+尺度) → 其余排除」；市内尺度 = **≤45 分钟且 ≤40 公里（AND）**，AND 是为了不误杀大阪→京都这类 30 分钟/42 公里的干线；**高铁名优先于尺度**，东京→品川 12 分钟的新干线照样显示。两道闸接在 GTFS 候选循环：**第一道**在取 trip 详情之前按制式+线路名排除（顺带省请求），**第二道**拿到经停表后按行程尺度复判。journey 带 `serviceClass`，图标层据此选**高铁=火箭头 / 干线=普通火车 / 长途大巴=大巴**（此前 route_type 判不出高铁，干线与新干线常常都是 2）。全部候选被排除时用独立错误码 `out_of_scope_service` + 被排除线路清单。**过程中测试与真机各抓到一个真 bug**：①`Number(null)===0` 而 0 在排除表里(Tram) ⇒「无 route_type」的线路被误判成有轨电车（Gautrain 复现）；②`gtfsOutOfScope` 声明在内层 try、却在兄弟 catch 使用 ⇒ ReferenceError。**归因修正（.27）**：范围过滤的结论曾被 12306 的 `station_not_found` 覆盖成「未在中国铁路车站表中找到该站名」——线路明明存在，正是 T-017/T-023 反复修掉的误导文案；现把 `out_of_scope_service` 作为**终态结论**，并**跳过付费长途源**（问了也只会返回市内线路）。**清单去重（.28）**：同一条线路在多个候选班次重复（丸ノ内線 40 班全属它），文案曾显示两遍。**旧 fixture 修正**：原 happy-path 假数据本身就是无 type/无名的线路，按新规则理应被排除，已改为「東海道新幹線/JR Central」并新增「丸ノ内線 fixture 必须报 out_of_scope_service」回归。**真实浏览器终验**（线上 `2026.09.25.28`）：東京→池袋 提示「找到的班次属于城市轨道或市内交通（丸ノ内線·地下铁/捷运），不在问乡海外的显示范围内」、**trip 请求 0 次**、付费源 0 次；大分→桜町 高速巴士仍可查（`glyphKind=coach` 大巴、2 次 trip）；线上跑分类器：新幹線→highspeed（含 12 分钟短途）、中央本線→rail、丸ノ内線→metro、山手線→commuter、市内公交→urbancoach、长途大巴→coach；控制台无 JS 错误；全量 86/86 | 待人工：①**新干线仍无数据源**（Transitland 无覆盖，需 Rome2Rio Key 或 NAVITIME），分类与火箭头图标已就绪；②`travelMode` 数字编码表待 Rome2Rio selftest 回来后校正；③轮渡/换乘暂不支持；④查询失败时地图仍残留上次结果；⑤Transitland Key 轮换；⑥**并发部署仍需加锁**（本轮继续用独立干净 worktree 绕开另一会话 WIP）；⑦**台账会被并行会话覆盖**（本行第一次就被 70baf8b 冲掉，需双方约定合并方式） | 2026-09-25 | OpenCode |

| T-035 | 海外长途/新干线数据源：用户否决全部现有方案，定规则「要么长期可靠数据，要么没有这个功能」；交接给 Codex | ✅ | 用户对 A（免费几何+站点，无时刻表）、B（契约试用/买断/按量）、C（长期谈判）**全部否决**，判定标准是**长期可靠的数据**，否则功能不存在。**已逐条核实并记录**（详见 `docs/HANDOFF_20260925_新干线数据源结论.md`，勿重复调研）：①Transitland 无日本新干线（实测「新大阪」0 站点）；②**ODPT 公共交通オープンデータセンター 死路**——JR 东日本 GTFS 目录原文「対象…一部の在来線…**新幹線は含みません**」且「チャレンジ2026限定」；③**駅データ.jp** 官网 FAQ 明确允许商用，但只有站/线路主数据、**无时刻表**，且免费版「新幹線駅のデータすべて」不含（¥4,400 买断含）、API 已停用；④**国土数値情報 N02** CC BY 4.0 **可商用**、有新幹線线路几何与车站，**无时刻表**（注意 N05 時系列**禁止商用**，勿拿错）；⑤**NAVITIME** 覆盖最好但 `train_data=timetable` 规格书明确「オプション契約が別途必要…APIマーケットでは利用不可」= 必须直接签约（90 天免费试用）；⑥**駅すぱあと API** 套餐表明载免费版与买断版**均无时刻表/运行信息**且「ダイヤ探索不可用」，时刻表只在契约版（90 天免费评价）；⑦駅探 法人 API 契约+免费试用；⑧Google transit 在日本覆盖存疑（**未验证**）；⑨Navitia 无 Key 访问 403、覆盖未验证；⑩抓取 Jorudan/Yahoo 违反 ToS → 明确不做。**代码现状**：T-034 的范围过滤（只显示城际/干线+高铁+长途大巴，排除地铁/捷运/通勤/市内公交）与 `serviceClass`（高铁=火箭头）**已上线并持续生效**；T-032 的 Rome2Rio 长途插槽**处于未启用状态**（服务器无 `longdistance.config.asp` ⇒ `capabilities.enabled=false`），海外查询只多发 1 次**免费**同源探测、`action=search` 0 次，**无第三方调用无费用**——它是「未启用的预留插槽」而不是功能。**发布状态**：本任务已全部上线（`.23`→`.24` CodePage 热修→`.26/.27/.28` 范围+归因+去重），线上 `2026.09.25.28`，**无待上传内容**；收尾时工作区剩余 dirty 全部属并行会话未提交 WIP，按规则 3 不得用于部署故未处理。**注意：台账会被并行会话覆盖**（T-034 行曾被 `70baf8b` 冲掉一次），合并时须保留 T-032/T-034/T-035 | 待 Codex/用户决策：①是否删除该**休眠**的长途插槽（默认建议保留：零成本、可配置关闭、接入点已测试；若删是小范围回退+一次发布）；②若未来真要该功能，前置条件是落实**长期**数据授权，属商务谈判而非技术任务；③其余遗留（查询失败时地图残留上次结果、Transitland Key 轮换、轮渡/换乘不支持）见 T-034 | 2026-09-25 | OpenCode |
| T-036 | 同步理解 OpenCode 全量改动并生成 ChatGPT 商业分析资料包 | ✅ | 已 fetch、确认 `origin/main` 是 HEAD 祖先；已读取本台账、当前生产清单、功能目录、架构文档与最近提交；已生成 `docs/chatgpt-commercial-analysis-20260925/`，覆盖产品、市场、技术、运营、风险、商业分析提纲及机器可读摘要，并验证 JSON 可解析且不含凭据值 | 将该目录中的 Markdown 文件与 `summary.json` 提供给 ChatGPT 开展商业分析 | 2026-09-25 | Codex |
| T-041 | 修复桌讯在线列表功能丧失（同时在线的人不出现，推荐/好友页全空白） | ✅ | commit `07758a9`/`bfbefcf`（`codex/desktalk-file-search-restore`），**已上线 `2026.09.26.8`（scope `desktalk-presence-identity`，3 文件 = `assets/js/desktalk.js` + `index.html` + manifest）**。**根因（生产实测实锤，非读代码推测）**：`api/dt_presence_mem.asp` 把 presence 身份绑定到 `Session("webwindows_user_id")`，**无会话时只接受 `guest_` 前缀的 ID，其余 `u` 一律静默丢弃**；而客户端同时踩两个坑——①心跳发 `?u=<昵称>`（昵称永不以 `guest_` 开头）；②心跳与列表都用 `credentials:'omit'`，ASP 会话 Cookie 根本不发送。两条件叠加 ⇒ **从来没有任何人被写入过 presence，在线列表必然永久为空**，与用户两张截图（推荐/好友页全空白）完全吻合。**线上取证**：`?u=NicknameProbe` → `{"ok":true}` 但 `list=1` 查不到；`?u=guest_probe1` → `list=1` 立即出现。**修复**：①心跳改发稳定 ID `u=me.id`（昵称降级为 `&name=` 展示元数据）且 `credentials:'include'`；②`fetchPresenceList` 同步带 `u=me.id` + `include`；③新增 `migrateLegacyFriendIds()`——旧版把昵称当好友 ID 存进 `localStorage.ww_friends`，改用稳定 ID 后会失配导致「好友」页仍空白，按「旧键 === 当前在线用户昵称」精确回键，**仅当该昵称唯一时迁移、重名跳过**，避免合并两人；④`index.html` 缓存戳升到 `?v=20260926-presence-identity-1`（服务器不发 `Cache-Control`，戳是唯一手段，同 T-030）。**顺带拆掉一颗地雷**：`api/dt_presence_mem.asp` 在**本车道与主分支都是 4498 字节加固前旧版**（无会话绑定、无 `guest_` 闸门、无「不被推荐」隐藏过滤、无旧昵称 ID 合并），而线上跑的是 7498 字节加固版、清单记录的也是 `9be07c74…`——任何包含该文件的发布会把隐私加固悄悄回滚。已把车道副本恢复为线上字节，**但刻意不纳入本次上传切片**（线上本来就是对的，无意义触碰）。**门禁**：新增 `tests/desktalk-presence-smoke.mjs`（同时钉客户端与**服务端契约**，服务端两条断言正是上面那颗地雷的哨兵）；`tests/deployment-manifest-smoke.mjs` 按仓库惯例注册新 scope 的精确切片；发布前 13 项相关冒烟全绿。**规则 5 备份对账**：`.deployment-backups/desktalk-presence-20260926-031115-2026.09.26.8/`，两个目标文件现网字节与上一版清单**逐字节相符**（desktalk.js `884e27e0`/106952 B、index.html `3bb04ddc`/33244 B）＝无第三方漂移。**清单**：`update-deployment-manifest.mjs 2026.09.26.8 --production … --files "assets/js/desktalk.js,index.html" --scope desktalk-presence-identity --preserve-catalog-version`（用 `--production` 使切片外文件一律沿用**线上**哈希，避开本地 91 个「仅换行」差异；`--preserve-catalog-version` 因功能目录未动，`system-apps.json` 保持 `2026.09.26.1`）。**上传**：内容 → 入口页 → manifest 末位，三文件均 HTTP 回读 SHA-256 相符，manifest 回读确认 `releaseVersion=2026.09.26.8`。**终验**：线上 `releaseVersion=2026.09.26.8`、scope 正确、两文件哈希与清单**三方一致**、线上 `desktalk.js` 确含 `credentials:'include'`+`u=me.id`+`migrateLegacyFriendIds`、`index.html` 缓存戳已生效、presence 接口按新客户端的 URL 形状实测可写入可回读；规则 8 入口依赖冒烟 **183 依赖全过**。回滚副本：远端 `.__previous_20260926-desktalk-presence` | **待办/须知**：①**用户须强刷桌面壳标签页**（常驻标签不自动重拉，同 T-030/T-033）；②`inc/conn.asp` 被加固版 `#include` 但**不在** `requiredFiles`/`integrity` 里（它确实在线上，`api/dt_discovery.asp` 已在用），建议纳入清单；③主分支 `desktalk.js` 完全没有 `dt_discovery.asp` 调用，「不被推荐」目前**只是本地 `ww_prefs`、服务端隐私开关从未被触发**——与 T-030 待办①「14 个线上新反向合并」同源，建议另立任务；④`dt_get_conv.asp`/`dt_send_link.asp`/`dt_fetch_links.asp` 线上实测**不绑定会话**，其 `credentials:'omit'` 无害，**不要无差别替换其它调用点**；⑤**并发事故（本次实锤）**：上传期间（03:11）并行会话在同一工作区 `cloud-layout-hotfix-20260926` 改动了 `file-query-parser.js`/`search-ui.js`/`files.asp`/`private-files.asp`/`file-search.css` 及两个测试，导致上传后 preflight 因「工作区不干净」失败——**但发布本身不受影响**（切片字节早已 commit 且回读校验通过）。`deploy/t041-backup-reconcile.ps1`、`deploy/t041-upload.ps1` 两个工具脚本**故意未提交**以免卷入并行会话的暂存区，发布负责人可自行纳入版本管理 | 2026-09-26 | OpenCode |

| T-042 | 同步本车道全部未提交改动、推送给 Codex 接手（无部署） | ✅ | 用户「最新的改动同步通知codex」。**规则 1 已执行**：`git fetch origin --prune`，`origin/main`(`35aced5`) 是 HEAD 祖先（`merge-base --is-ancestor` = 0），分支 `codex/WebWindows-开发者工具`，同步前 HEAD `68d9aa8`（领先上游 2）。**提交 2 个**：①`185b83d` test(e2e) Playwright 端到端冒烟——`playwright.config.mjs`（`testDir=tests/e2e`、`channel:'chrome'` 用本机 Chrome 不下内核、`webServer` 自动拉起静态服务器）、`tools/static-server.mjs`（零依赖 node 静态服务器 :4173，**不执行经典 ASP**）、`tests/e2e/static-pages.smoke.spec.mjs`（首页桌面/任务栏/开始菜单 fallback 渲染 + `main.js`/`app-registry.js` 200 + 同源非服务端资源 0 失败、about/services/contact 二级页、404 页）、`package.json` 增 `test:e2e`/`serve:static` 与 `@playwright/test@^1.63.0`；**实测 `npx playwright test` 3/3 通过（8.1s）**。②`c238c4f` docs——`STORAGE_CAPABILITY_V1_DESIGN.md` 新增第 18 节「Phase 3 选择器与卷生命周期冻结决策记录」（每 Activity 唯一 `StoragePickerOperation` + `picker-busy`、取消/替换/失败语义、导航世代作废与 120s 超时墓碑、`(exactOrigin, opaqueVolumeId)` 分区与一次性显式迁移）；新增 `DREAMA_RUNTIME_KERNEL_DESIGN.md`（内核索引与继续点：Storage v1 是唯一未冻结能力）；**把 T-036 产出的 `docs/chatgpt-commercial-analysis-20260925/`（5 篇 md + `summary.json`）补提交入库**——此前一直是未跟踪状态，随时可能丢（zip 因 `*.zip` 忽略未入库）；`cloud/desktalk/groq.env.example` 密钥模板入库。③顺带给 `.gitignore` 加 4 条：`.worktrees/`、`.codex-worktrees/`、`.deployment-backups/`、`deploy/production-backups/`——此前 `git status` 被数百个生产备份文件（含 prod 凭据副本）淹没，既拖慢并行会话 preflight，也有误提交泄露风险。**刻意未提交（留给归属方，规则 3）**：`WebWindows` 子模块 dirty（`assets/js/main.js`、`webwindows-vue/src/desktop/{Taskbar,WindowManager}.vue`、package.json/lock；且父仓无 `.gitmodules` 映射，gitlink 只能记 commit）；`mail-center-net47/AccountStore.cs`+`ApiHandler.cs` 未跟踪（csproj 为 SDK 风格会自动包含，但**是否被 `Api.ashx` 引用未核**，需归属方确认后入库，否则 net462 项目缺文件编译不过）；`webwindows.zip`（74MB 归档）被删除但未提交。**本轮无部署**：改动全在 docs 与测试基建，无可上线内容，`deploy/ftp-manifest.json` 未动、未占版本号、未跑 preflight；生产仍为 `2026.09.26.8` / scope `desktalk-presence-identity` | Codex 接手：`git fetch --prune` + `git pull`（分支 `codex/WebWindows-开发者工具`，HEAD 将为 `c238c4f`）后按第 2 节遗留继续。待决/须知：①`WebWindows` 子模块 WIP 归属与提交方式（父仓无 `.gitmodules` 映射，gitlink 提交需先定基线 commit）；②`mail-center-net47/*.cs` 是否入库（不入则 net462 编译缺文件）；③`webwindows.zip` 到底是删还是恢复，别让 74MB 长期挂在 diff；④`npm run test:e2e` 可作后续页面改动门禁，但**不执行 ASP**、依赖本机已装 Chrome，且与 86 项 node 冒烟互补不替代；⑤台账并发覆盖问题仍未根治（T-034/T-041 行均被并行会话冲掉过），合并时须保留 T-032/T-034/T-035/T-041/T-042；⑥T-041 遗留（线上 14 个新文件反向合并、`inc/conn.asp` 纳入清单）与 T-003/T-007 仍未闭环 | 2026-09-26 | OpenCode |

| T-043 | 后台登录验证码 500 + 登录页 `Unexpected token '<'` 修复（接手 Codex 半途），上线 `2026.09.26.12` | ✅ | **根因（生产实测实锤）**：`admin_api/adminAuth.asp?action=captcha` 恒 500，IIS 原文「错误 'ASP 0126' 找不到包含文件 "/admin_api/adminAuth.asp"，行 3：找不到包含文件"../inc/admin-security.asp"」；`/inc/admin-security.asp` HTTP 404、FTP 也不存在，而**线上 `admin_api/adminAuth.asp` 字节 == 仓库**（T-030 回补 35 个文件时把「带 include 的端点」传上了线，**include 片段本身从未进 requiredFiles、从未上传**）⇒ 与 T-031 的 `inc/trust-schema.asp` 同一类事故的第二次。**审计**：全仓 66 个 ASP 的 SHTML include 全量比对，缺失仅 4 项 —— `inc/conn.asp`（真实 DB 凭据，永不纳管，T-033）、`inc/admin-security.asp`（本次补）、`*.config.asp`（transit/longdistance/chatproxy 密钥，永不纳管）、`inc/validator-deployment-config.asp`（其 includer `developer_api/v1.asp` 本身未上线）。**登录页整条链也从未纳管**：`SystemManager/login.html`、`SystemManager/assets/{js/admin-login.js,css/admin-login.css}` 三件都不在清单里，其中线上 `admin-login.js` 是 T-003 加固前的旧版（2927 B，无 `csrfToken`）——即使 500 修好，旧客户端也会在登录 POST 缺 `X-WebWindows-CSRF` 被 403 拒绝，**必须成套补齐**（Codex 的判断正确）。**修复**：①新增门禁 `tests/deployment-asp-include-smoke.mjs`（已部署 ASP 的每个 SHTML include 目标必须被清单纳管，服务端凭据配置逐条显式白名单、不允许通配豁免整个 `inc/`；钉住后台登录链纳管 + `admin-security.asp` 哈希 == 仓库；再钉「每个 includer 首行必须声明 `CodePage=65001`」，7 个 admin 端点全覆盖）；②`admin-login.js` 新增 `readJsonPayload`，IIS/网关的 HTML 错误页转成可读中文（`ADMIN_RESPONSE_NOT_JSON`），不再抛 `Unexpected token '<'`；③`login.html` 缓存戳 → `?v=20260926-admin-login-1`（同 T-030/T-041 惯例）；④清单登记 4 文件，scope `admin-login-security-repair`（`tests/deployment-manifest-smoke.mjs` 登记精确切片；`deployment-entry-dependency` 修正 `href="/"` 被当成待纳管文件的既有 bug）；⑤`tools/update-deployment-manifest.mjs` 移植 `--preserve-catalog-version`，且与 `--production` 同用时**目录版本取线上真相**（否则会把线上 .26.1 记回本分支陈值）；⑥`data/apps/system-apps.json` 反向同步线上字节（catalogVersion 2026.09.26.1，只作清单记录基线、不进上传集）。**门禁与部署**：preflight_ok（release .12 / production .11 / 266 files / 5 upload）；规则 5 备份于 `.deployment-backups/admin-login-repair-20260926-130643-2026.09.26.12/` —— `inc/admin-security.asp` = NEW_FILE（远端确认不存在）、另 3 件 = UNMANAGED_BEFORE 且生产字节与 HTTP 取回副本哈希一致、线上清单 FTP 字节 == 观测清单（`43939dc0`，无 TOCTOU）；上传走「临时名 STOR → 临时名回读 → RNFR/RNTO → 正式名回读」5/5 全过，线上 5 文件哈希与本地清单**三方一致**，首次失败遗留的临时名已 DELE 清理。**新踩的 FTP 坑（值得写进惯例）**：①`RNTO` 目标已存在时 IIS FTP 回 **550 Cannot create a file when that file already exists**（`deploy/ftp-manifest.json.__bak` 每次发布都存在 ⇒ 每次都要先 `DELE` 旧备份，否则卡在 backup-rename）；②`RNFR/RNTO` 路径必须相对**登录根**（`wwwroot/deploy/...`），写成 `/deploy/...` 会失败（curl exit 21）。**终验（真实浏览器 + curl）**：验证码按钮返回 `9 + 8 = ?`（原为 500 HTML）；CSRF 门三连实测 `CSRF_REQUIRED`/`CSRF_INVALID`/`ADMIN_ORIGIN_INVALID` 全 403；带正确 CSRF + 错误口令 → 401 且页面显示可读中文「管理员账号或密码错误。」，失败后自动换验证码，**控制台除预期 401 外无任何错误、无 `Unexpected token`**（截图已存档）；`api/release-version.asp` = 2026.09.26.12 / catalog 2026.09.26.1；随 include 补齐，`deleteDatacenter/saveDatacenter/visitorAnalytics` 等端点的 500 一并解除。**测试**：全量 86 过；2 个失败为**工作树环境所致、与本次改动无关**（`developer-center-docs-smoke` 需要被 `*.zip` 忽略的 `developer-samples/hello-webwindows.zip`；`production-deployment-rehearsal-smoke` 报 `.gitattributes` 字节漂移，worktree 检出 386 B vs 主工作树 370 B），已在 c37d60d 的干净 worktree 复证同样失败。**分支**：独立干净 worktree `.codex-worktrees/admin-login-repair-20260926`，分支 `codex/admin-login-security-repair`，部署 commit `922422c` + 门禁补充 commit `4bf2cf1`（均已推送） | 待人工/遗留：①**正确口令的登录成功路径未验证**（我无口令，不应尝试），请用户自己登录一次确认；②直接访问 `/inc/admin-security.asp` 仍 500（VBScript 800a0401，纯 include 片段的既有约定——`inc/conn.asp`/`inc/trust-schema.asp` 直接访问是 200；要改成 200 得给 include 加 `@` 指令，风险高，建议不动，只保留门禁）；③仍未纳管的后台文件：`admin_api/{getUsers,getUserById,saveUser,developerPlatform,functionCatalog}.asp`、根目录 `admin-login.html`（那是 tailwind 旧壳，属 T-004，与 `SystemManager/login.html` 无关）；④`deploy/ftp-manifest.json` 在主车道 `codex/WebWindows-开发者工具` 仍停在 2026.09.25.28，Codex 后续发版请继续以线上清单为 `--production` 基线；⑤远端 `.__bak` / `.__previous_*` 备份积压待清理，见 T-002 | 2026-09-26 | OpenCode |

| T-044 | T-043 回归核查中发现的两个既有缺陷（码页缺失导致 500 / 读接口无鉴权），**未修**，待用户决策 | ⏸️ | T-043 上线后做「是否影响其它东西」的回归核查：①清单级证明 .11→.12 **261 条记录逐字节不变、0 变更、0 删除**，只新增后台登录 4 文件，目录版本不变；②清单内 23 个 ASP 全量 HTTP 扫描，admin_api 全部返回设计码（405/403/401/200），无 500；③真实浏览器回归：首页完整启动、settings 零控制台错误、SystemManager 壳零错误；④用户截图里的 4 个红色报错**全部来自浏览器扩展**（`chrome-extension://…/manifest.json` web-accessible-resources 拒绝、`pino-*.js` ERR_FAILED、MaxListeners/`>0 ObjectMultiple` 警告），站点侧零错误。**据此新发现的两个既有缺陷（都不是 T-043 造成，字节未变、不在本次切片内，故未动）**：**(A) `cloud/browser/node-config.asp` 线上恒 500** —— VBScript `800a0409 未定义的错误`，第 66 行 `CloudUiMessage = "ファイルの場所が無効です。"`，该文件是**日文**字面量且首行没有 `<%@LANGUAGE=…CODEPAGE=65001%>`，IIS 按系统 ANSI（本机 936）读无 BOM 的 UTF-8 → 多字节被拆坏；与 T-032 longdistance-proxy、今天 `inc/admin-security.asp` 同一类事故（`tests/deployment-asp-include-smoke.mjs` 只钉住了 includer 的码页前置条件，**没覆盖这种「独立页面自带非 ASCII 字面量」的情形**）。**(B) `admin_api/getUsers.asp`、`admin_api/getUserById.asp` 完全没有鉴权** —— 两者只 include `inc/conn.asp`，不带 `inc/admin-security.asp`，无 session/header/CSRF 校验；实测未登录直接 GET `getUsers.asp` 返回 4 条用户记录（username / nickname / avatar / email / data_center_id / data_center_name / expired_at）。**已核实不含 password 字段**（SQL 是 `u.*` 但序列化逐字段列举，未写 `rs("password")`），所以不是密码哈希泄露，但用户名单与邮箱对公网公开，仍是应修的信息泄露。对照：`saveUser.asp` 虽已带闸门，但只在 POST 生效，GET 仍走参数校验（返回 200 `密码不能为空`），属既有设计而非回归 | 待用户/Codex 决策是否另开一次**隔离**修复：①给 `node-config.asp` 首行补 `CodePage=65001`（一行，风险极低；可考虑把门禁从「includer 前置条件」扩展到「所有含非 ASCII 字面量的 ASP 必须声明码页」）；②给 `getUsers.asp`/`getUserById.asp` 补 `AdminSecurityRequireProof` 读鉴权（需确认 `SystemManager/assets/js/users.js` 已带 `X-WebWindows-Admin-Request` 与 CSRF 流程，且这两个文件目前**不在清单内**，要一并纳管）。两项都未擅自实施，避免在 T-043 之外扩大影响面 | 2026-09-26 | OpenCode |

| T-045 | 后台首页「样式丢失」修复：外壳与数据中心页脱离 195B tailwind stub，上线 `2026.09.26.13` | ✅ | 用户报 `https://www.y0.hk/systemmanager/index.html` **样式丢失**（截图里左侧深色侧栏消失、导航挤成顶部一行）。**根因**：`SystemManager/index.html` 与 `SystemManager/datacenter.html` 都引用 `../assets/css/tailwind.min.css`，而线上 `/assets/css/tailwind.min.css` **只有 201 字节** —— 内容是 `body{margin:0;font-family:sans-serif}` + `.text-xs` + `.text-4xl` + `.mt-1` 四条规则的 fallback（就是 T-004「主线 3 个页面引用 195B tailwind stub」那条一直没修的项）。外壳布局全靠 Tailwind 工具类（`flex h-screen overflow-hidden` / `w-64 bg-gray-800 text-white` / `flex-1 overflow-y-auto space-y-2` / `hover:bg-gray-700` …），4 条规则里一个都没有 ⇒ 侧栏、间距、悬停色全部落空。**注意**：外壳的 iframe 内容（`dashboard.html`）看起来是正常的，因为它早就引用了真实 Tailwind —— 这正好反证问题只在外壳本身。**审计**：后台 12 个页面里只有这 2 个指向 stub；`dashboard/users/layout` 早已用 jsDelivr `tailwindcss@2.2.19`，`feedback/news/settings` 用 `cdn.tailwindcss.com`（v3）。**修复**：这两页改用**与兄弟页完全一致**的 `https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css`；逐类核对两页用到的 **44 个工具类在 2.2.19 中全部存在**（含转义写法 `.hover\:bg-gray-700`、`.hover\:text-white`、`.flex-shrink-0`、`.h-screen`）。**刻意不用 `cdn.tailwindcss.com`**：那是 v3，已把 `flex-shrink-0` 改名 `shrink-0`，外壳标记按 2.x 写，换 v3 会再坏一次；新门禁把「不得改成 v3 CDN」也钉住。**验证**：用仓库零依赖静态服务器 + **生产外壳真实标记**渲染实测 —— `aside` 宽 **256px**、背景 `rgb(31,41,55)`、白字、`body` display:flex/overflow:hidden，深色侧栏与仪表盘卡片布局恢复（已截图存档）；线上 CDN 200 / 2,934,019 字节可取。**门禁与部署**：新增 `tests/admin-console-tailwind-smoke.mjs`（线上页面一律不许引用 stub、外壳必须加载 2.2.19 且不得换 v3、守住外壳布局类不被删、断言 stub 仍是 <4KB 的 fallback）；`deployment-manifest-smoke` 登记 `admin-console-tailwind` 精确切片；preflight_ok（.13/.12、266 files、3 upload）；规则 5 备份后**两页生产字节与上一版清单逐字节 MATCH**（无第三方漂移）、清单 FTP 字节 == 观测清单；上传 3/3 正式名回读全过；线上 `release-version.asp` = 2026.09.26.13 / scope admin-console-tailwind，两页线上 head 已确认指向 2.2.19。全量 **87 过**（比上次多 1 项即新门禁）；2 个失败仍是工作树环境问题（缺被 `*.zip` 忽略的 `developer-samples/hello-webwindows.zip`、`.gitattributes` 换行漂移），已在 c37d60d 干净 worktree 复证。**分支**：`.codex-worktrees/admin-console-tailwind-20260926` / `codex/admin-console-tailwind`，commit `1bdc56e`（已推送），主车道已快进合并 | 待人工/遗留：①**用户刷新后台外壳（Ctrl+F5）确认观感**——我只验证到未登录会跳登录页，登录态的最终观感需要你登录后看；②`assets/css/tailwind.min.css` 这个 201 字节 stub 仍在清单里（`aiwork/*` 5 个未上线的设计稿页面还在引用它，删了会一起坏），建议保留但**任何线上页面都不许再引用**（已由门禁保证）；③若要彻底去掉 CDN 依赖，需要给后台页建 Tailwind 构建（新增 devDependency + 配置 + 提交构建产物），属独立工程，本次未做；④T-044 里的两个既有缺陷（`cloud/browser/node-config.asp` 码页 500、`getUsers/getUserById` 无鉴权）仍未修；⑤`SystemManager/news.html` 重复引用 `news.js` 引发 `SyntaxError: Identifier 'quill' has already been declared`，且该页是静态演示数据 —— 建议并入 T-044 一起处理 | 2026-09-26 | OpenCode |

| T-046 | 访客统计：地区解析（外部 IP API）+ 世界地图下钻到地级市 + 设备/停留时间/趋势图表，上线 `2026.09.26.14` | ✅ | 用户诉求：「访客地区要能显示出来，有个世界地图的色彩分布（精确到市区），图表能看到设备、停留时间」。**关键发现（先查再写）**：数据管线其实**早就建好了** —— `webwindows_visitor_sessions` 已有 `country_code/country_name/region_name/city_name/device_type/active_seconds` 字段，页面也已在渲染「近似地区/设备/活跃时长」列；地区四列为空是因为 `api/visitor-analytics.asp` 只在环境变量 `WEBWINDOWS_ANALYTICS_TRUST_IIS_GEO` 开启时读本机 IIS GeoIP 的 `GEOIP_*`，而生产没开（该约束写在 `data/deploy/production-environment-config-v1.json`，明令禁止信任转发头）。**用户决策**：地区来源用**外部 IP 解析 API**（我提示过会把访客 IP 发给第三方、免费额度有限，用户明确选择）；地图要**世界国家色阶 + 中国下钻到市区**。**选型实测**（都现场打过真实请求）：`ipwho.is` HTTPS 无需 key、`lang=zh-CN` 直接给中文省市名（实测 114.114.114.114 → 中国/江苏省/南京），官方文档确认免费档**每客户端 IP 每日 1,000 次**、允许商用；`ipapi.co` 免费档已 RateLimited；`db-ip` 只有英文名（与 DataV 中文区划名对不上）。地图数据：世界用 Natural Earth `ne_110m_admin_0_countries.geojson`（jsDelivr 代理 GitHub，**177 个要素里 175 个带 `ISO_A2_EH`**，与库里的 `country_code` 直接对齐，因此不需要任何国家名映射表）；中国用 DataV 阿里云 `areas_v3/bound/{adcode}_full.json`（无需 key，实测 `100000_full`=35 省、`320000_full`=江苏 13 市，均带 `adcode`/`centroid`/`childrenNum`）；两者 CORS 都是 `*`。**服务端**：`api/visitor-analytics.asp` 地区优先级 ① 本机 IIS GeoIP（既有开关，行为不变）② 同 IP 历史会话缓存（重复访客 0 次外部调用）③ 外部 API；硬约束：只用 `REMOTE_ADDR`、内网/回环/链路本地不外发、每日外部调用硬上限（Application 计数，默认 800，低于免费额度）、2.5–3s 超时、**任何失败 fail-open**（地区留空、统计照常写入，绝不因地区解析丢会话）、只接受 https、供应商字段用别名归一（换源只改配置不改代码）。端点与可选 key 放服务器端 `api/visitor-analytics.config.asp`（`.gitignore` 已覆盖 `api/*.config.asp`），新增入库模板 `api/visitor-analytics.config.example.asp`；**服务器上没有该文件时代码路径与改动前完全一致**（只多一次 `FileExists`），属显式启用、默认零风险。`admin_api/visitorAnalytics.asp` 新增 `geo`（来源 + 已解析/总会话数）、`devices`、`dwell`（6 档直方图单次查询）、`daily`（`DATE_FORMAT` 每日趋势）、`countries`（按 ISO alpha-2）、`chinaRegions`（省市）。**前端**：新增 `SystemManager/assets/js/visitor-analytics-charts.js`（ECharts 5.5.1）—— 世界地图按会话着色、点中国下钻省→地级市、面包屑可回退，设备环形图、停留时间柱状图、每日趋势（会话/独立访客双轴 + 活跃时长柱）。**诚实呈现两处**：Natural Earth 110m 下香港/澳门/新加坡没有独立边界要素，这些地区的数据**不静默丢失**而是列在地图下方；IP 解析最细到地级市，因此**不再往区县下钻**（避免把市级数据摊到区级）。页面与 CSS 只补 `h-map/h-72/mb-2` 三个手写工具类（该页样式自包含、不引 Tailwind），缓存戳升 `?v=20260926-1`。**真实渲染验证**（零依赖静态服务器 + 生产真实标记/真实 CSS + 模拟 payload）：世界地图按会话正确着色（CN 最深）、下钻 `世界 › 中国 › 江苏省` 面包屑与着色均正确（南京 8 / 苏州 3，与数据一致）、设备/停留/趋势三图正常、控制台零错误。**过程中抓出并修掉两个真 bug**：①`series.data` 的 `name` 写成了展示名（中文）导致**整张世界地图不着色** —— ECharts 是靠 `name` 匹配地图区域的；②点击下钻后**面包屑不刷新**（只在初次 render 时渲染）。**门禁与部署**：`tests/visitor-analytics-smoke.mjs` 扩展 40 余条断言（含「代码里不得出现供应商域名」「不得读任何转发头」「端点必须 https」「内网不外发」「必须复用同 IP 缓存」「必须有每日上限」）；全量 **87 过**（2 个失败仍是工作树环境问题：缺被 `*.zip` 忽略的 `developer-samples/hello-webwindows.zip`、`.gitattributes` 换行漂移，已在 c37d60d 干净 worktree 复证）；顺手修掉一个身份链隐患：新文件初版有 26 行 CRLF，清单哈希取自 CRLF 字节而 git blob 是 LF（`.gitattributes` 声明 `*.js text eol=lf`），已统一为 LF 并重算，现清单哈希 == 工作区字节 == git blob 三方一致。preflight_ok（.14/.13、267 files、7 upload）；规则 5 备份后 6 个既有文件生产字节与上一版清单**逐字节 MATCH**、`visitor-analytics-charts.js` 为新增（UNMANAGED_BEFORE）、清单 FTP 字节 == 观测清单；上传「临时名 STOR → 双回读 → DELE 旧 `.__bak` → RNFR/RNTO → 正式名回读」7/7 全过；线上 7 文件哈希 == 本地清单全 MATCH；`api/visitor-analytics.asp` 无效会话仍 400（热路径未受影响）；服务器上确认**没有**真实配置文件 ⇒ 地区外部解析处于未启用状态。**分支**：`.codex-worktrees/visitor-geo-map-20260926` / `codex/visitor-geo-map`，commit `8a667c0` + `99a32dc`（均已推送），主车道已快进合并 | **待用户做（一步）**：想让地区与地图出数据，需把 `api/visitor-analytics.config.asp` 传到服务器 `api/` 目录，内容照 `api/visitor-analytics.config.example.asp`（默认 `geoApiBase = "https://ipwho.is/{IP}?lang=zh-CN"`、`geoApiKey = ""`、`geoDailyCap = 800`）。**该文件不入库、不在上传集**（`.gitignore`），上传后**新访客**的会话才会带地区；历史会话不会被回填。若额度/供应商要换，只改这个文件即可，不用改代码。**待人工**：①登录后台打开访客统计页确认地图与三张图（我只验到未登录会跳登录页，登录态观感需要你看一眼）；②若想连香港/新加坡也上色，可把世界底图换成 Natural Earth 50m（3MB，`WORLD_GEOJSON` 常量一行的事）；③若日后拿到 ipwho.is 付费 key 或换供应商，填 `geoApiKey` 即可；④`cloud/browser/node-config.asp` 码页 500 与 `getUsers/getUserById` 无鉴权仍未修（见 T-044）；⑤`SystemManager/news.html` 重复引用 `news.js` 的 `SyntaxError` 仍未修 | 2026-09-26 | OpenCode |

| T-047 | 「每个窗口停留时间看不到」：先证伪（数据没坏）→ 上移为第一区块 + 补每窗口图表，上线 `2026.09.26.15` | ✅ | 用户反馈「每个窗口的停留时间看不到了，想统计每个窗口的停留时间」。**先证伪再改**：用**生产真实页面脚本 + 模拟 payload** 在本地跑了一遍，数据与渲染都正常 —— 「功能使用与停留时间」表格确实出行（系统设置 1 小时 30 分 / 相机 1 小时 10 分 / 桌讯 43 分 20 秒 / 问道 15 分），`#featureRows` 有 4 行、`#featureChart` 之外一切正常。**真因**：T-046 新增的世界地图（480px）+ 设备/停留时间/每日趋势三张图，把这个表格挤到了**页面第 7 个区块**，要滚过 5 屏才看得到，于是「像是消失了」。**改动**（只动访客统计页四件套，**不碰采集与聚合接口**）：①区块顺序 —— 「每个窗口停留时间」从页面末尾提到 KPI 摘要之后成为第一块，地图与三张图下移其后；②新增横向条形图 `#featureChart`：按累计活跃停留倒序取前 12 个窗口，条尾标注可读时长，tooltip 给累计停留/打开次数/会话数/平均每次，`#featureSummary` 说明「共 N 个有记录，展示前 M 个，合计 X」；③表格 4 列扩到 6 列：功能窗口/打开次数/会话数/累计停留/**平均每次**/**停留占比**（带迷你占比条），空态 colspan 同步改 6；④CSS 补 `.h-feature`/`.h-2`/`.bg-sky-500`，该页样式仍自包含不引 Tailwind；⑤缓存戳升 `?v=20260926-2`。**门禁新增顺序守卫**：`featureChart` 必须出现在 `geoMap` 之前（这次的问题根源就是区块顺序），并断言 `featureRows` 在页面里**只出现一次**（防再次复制出两个表）。**验证**：本地零依赖静态服务器加载生产真实页面与脚本，注入模拟 payload 后确认区块顺序为「每个窗口停留时间 → 访客地区分布 → 设备分布 → …」、图表 canvas 存在、表格行含平均每次与停留占比、区块距页面顶 558px（首屏可见），控制台零错误；全量 **87 过**（2 个失败仍是工作树环境问题）；preflight_ok（.15/.14、267 files、5 upload）；规则 5 备份后 4 个文件生产字节与上一版清单**逐字节 MATCH**、清单 FTP 字节 == 观测清单；上传 5/5 正式名回读全过；线上 release `2026.09.26.15` / scope `visitor-window-dwell`，5 文件哈希与清单全 MATCH，页面/charts.js/css 均 200，采集热路径仍 400（未受影响）。**分支**：`codex/window-dwell-chart`，commit `494f242`（已推送），主车道已快进合并 | 待人工：①登录后台打开访客统计页确认「每个窗口停留时间」已在第一屏（我只验到未登录会跳登录页）；②窗口停留的统计口径是「页面可见且用户未空闲」的累计活跃时长，因此后台标签页长时间挂着不计入 —— 若你要的是「窗口打开到关闭」的墙钟时长，需要另一个口径，请明确后我再加；③若某窗口不出现，说明该窗口这段时间没有上报（客户端只在可见且活跃时 pulse）；④T-044 的两个既有缺陷（`cloud/browser/node-config.asp` 码页 500、`getUsers/getUserById` 无鉴权）与 `news.html` 重复引用 `news.js` 的 `SyntaxError` 仍未修 | 2026-09-26 | OpenCode |

| T-048 | 世界地图没有地区：根因=地区解析未启用（服务器缺配置）→ 启用+抽共享模块+一键补全历史地区；**同轮修掉我自己引入的源码回显事故**，上线 `2026.09.26.16` / `2026.09.26.17` | ✅ | 用户反馈「有数据访问记录，世界地图怎么不能显示访问地区了」。**根因不是前端**：地区解析是**显式启用**的 —— 只在 `WEBWINDOWS_ANALYTICS_TRUST_IIS_GEO` 开启时读本机 IIS GeoIP，否则读服务器端 `api/visitor-analytics.config.asp`；生产两者都没有（FTP 列表确认 `api/` 下只有 transit/flight/navigation 三个 config，没有 visitor-analytics.config.asp），所以 `webwindows_visitor_sessions` 的地区四列**从头到尾都是空的** —— 访问记录是真的，只是每条都没有地区可着色。T-046 我说过「没有配置文件时行为与改动前完全一致」，但没把「你必须自己上传它才看得到地区」讲醒目，这次补上：**页面上方直接红色写明原因 + 给按钮**。**改动**：①抽出共享模块 `inc/visitor-geo.asp`（配置加载/内网过滤/同 IP 缓存/每日额度/HTTP 调用/fail-open），采集端与管理端共用，避免两份实现漂移；采集端删掉内联副本与失效的 `EnvironmentFlag`；②管理端新增 `action=backfill-geo`：管理员一键回填**启用之前就产生**的会话地区（POST + `AdminSecurityRequireMutation "visitor-analytics"`，单次上限 200 地址、复用同一份每日额度，返回 scanned/resolved/skippedPrivate/failed/remainingBudget；未启用时明确 `GEO_NOT_CONFIGURED`）；③读取接口新增 `geo.pendingAddresses`；④页面加「补全历史地区」按钮并引入既有 `admin-security.js` 走现成 CSRF 流程；缓存戳 `?v=20260926-3`。**服务器配置已由我上传**：`api/visitor-analytics.config.asp`（`geoApiBase=https://ipwho.is/{IP}?lang=zh-CN`、`geoApiKey=""` 空、`geoDailyCap=800`；**不含任何密钥**，内容来自已入库的 `api/visitor-analytics.config.example.asp` 模板；该文件按设计不入库、不在上传集，走临时名 STOR→回读→RNFR/RNTO 翻转，线上字节 == 本地）。**同轮我引入并修掉的事故（重要）**：`inc/visitor-geo.asp` 的注释里写了 Language 码页指令的样子，那个 ASP 定界符**提前结束了 include 的脚本块**，导致该文件剩余源码被当成正文回显 —— 公共采集接口 `/api/visitor-analytics.asp` 的响应从 `{"ok":true}`（11 B）变成 **11,006 B 的 include 源码**（会话写入仍成功，但响应不是合法 JSON 且泄露服务端源码给任何能 POST 该接口的人）。**为什么没被拦住**：VBScript 侧完全合法，`cscript` 编译期检查只查 800A03xx，测不到定界符配对问题。**修复**：注释不再出现任何定界符 + 新增 `tests/classic-asp-include-fragment-smoke.mjs`（解析全部 `<!--#include -->` 目标，断言每个被引用文件是单一脚本块且块内含注释都不得再出现定界符；另断言任何 tracked .asp 的注释行都不得含定界符），并**用「把事故写法注入回去」验证门禁确实会失败**再还原。`.17` 上线后复测：采集接口响应回到 **11 字节 `{"ok":true}`（合法 JSON）**，405 路径 40 字节纯 JSON。**其它验证**：preflight 通过（.17/.16、268 files、7 upload）；规则 5 备份后 6 个既有文件生产字节与上一版清单**逐字节 MATCH**；上传 7/7 双回读全过；未登录时 summary/backfill(GET/POST) 全部 401（证明重构后 ASP 可编译且 CSRF 门禁有效）；`/inc/visitor-geo.asp` 直访 200；连续上报 400–800ms 且新会话命中同 IP 缓存（0 次外部调用）。全量 **88 过**（新增 1 项即该门禁；2 个失败仍是工作树环境问题）。**分支** `codex/visitor-geo-backfill`，commit `10bee0a`/`8ae9141`/`82a2cf9`（均已推送），主车道已快进合并 | **待人工**：①登录后台打开访客统计页 —— 顶部状态条会直接显示「地区来源：外部解析 API（已解析 N/M 个会话）」，地图下方如有待补全地址会黄字提示；**历史记录需要点一次「补全历史地区」**（我没有管理员口令，无法代点，也因此**无法确认 DB 里地区是否已写入** —— 请你点一次并确认地图上出现颜色；若有地址失败，多点几次即可，受每日 800 次额度限制）；②新访客的会话会自动带地区，无需操作；③若地图仍空且状态条显示「已解析 0」，把状态条原文发我，我按 `geo.source`/`pendingAddresses` 定位；④外部解析的隐私含义（访客 IP 会发给 ipwho.is）此前已征得你确认；⑤T-044 的两个既有缺陷与 `news.html` 的 `SyntaxError` 仍未修 | 2026-09-26 | OpenCode |

| T-049 | 世界地图仍然没有地区：真根因=共享 include 里 `Server.MapPath` 相对路径解析到 `/inc/`，外部解析**从未真正发起**；路径改为调用方传入，上线 `2026.09.26.18` | ✅ | 用户反驳得对：T-048 我给的结论（「地区解析未启用」）**不完整**。配置我确实已上传到 `api/`，但地区仍然全是「未知」—— 说明代码在 HTTP 调用之前就退出了。**这次不靠猜，用可判定实验定位**：①把 `geoApiBase` 指向不可路由地址 `https://10.255.255.1`，配置只在 `/api/` 时请求恒为 **~550ms**（既不出现连接超时，也从不写地区 ⇒ HTTP 根本没被调用）；同一时刻本机直连供应商 **245ms 返回 JP/東京都**（供应商可用）；②把同一份配置**同时放到 `/inc/`**，请求立刻变成 **2174ms** 的连接超时 ⇒ 路径问题被证实。**真根因**：`inc/visitor-geo.asp` 里 `Server.MapPath("visitor-analytics.config.asp")` 的相对路径是相对**本 include 片段所在目录**（`/inc/`）解析的，不是调用方所在的 `/api/`。配置文件一直在 `api/` 下，于是每次都判定「未配置」直接 return —— 外部解析从来没有真正执行过。这也是 T-048 里「管理端 `geo.source` 会误报未启用」的同一个坑（管理端在 `/admin_api/`，同样解析不到）。**修复**：新增 `GeoConfigureSub`，由调用方把**已解析的物理路径**传进来（采集端 `Server.MapPath("visitor-analytics.config.asp")`，管理端 `Server.MapPath("../api/visitor-analytics.config.asp")`），共享模块不再自行解析相对路径（`GeoConfigPath` 为空即判定未配置）。**门禁新增三条断言**：共享模块代码（忽略注释行，因为文件里保留着事故说明）不得出现相对 MapPath 解析配置、两个调用方都必须显式传入路径。**修复后复测同一实验**：不可路由端点下首次请求从 561ms 变 **1907ms**（HTTP 调用确实发生了），真实端点下 500–1500ms 波动；`.18` 上线后采集接口仍返回 **11 字节 `{"ok":true}`**（无回归）。**服务器最终状态已核对**：`api/visitor-analytics.config.asp` 存在且内容为 `geoApiBase = "https://ipwho.is/{IP}?lang=zh-CN"` / `geoApiKey = ""` / `geoDailyCap = 800`；`inc/` 下无任何 config 残留（探测副本已删）。**门禁与部署**：preflight_ok（.18/.17、268 files、4 upload）；规则 5 备份后 3 个文件生产字节与上一版清单**逐字节 MATCH**、清单 FTP 字节 == 观测清单；上传 4/4 双回读全过；全量 **88 过**。**顺带发现并规避的仓库陷阱**：`dist/index.html` 入库时是 CRLF 而 `.gitattributes` 声明 `eol=lf`，在干净 worktree 里执行 `git add -A` 会产生 **443 行假差异**（内容其实与 HEAD 逐字节相同，sha 一致），并让 preflight 以「工作树不干净」失败 —— 本次已 `git reset` 剔除 + `update-index --assume-unchanged` 规避，并**改为只显式 add 本次文件**。**分支** `codex/visitor-geo-path-fix`，commit `603f11e`（已推送），主车道已快进合并 | **待人工（关键）**：①**登录后台点一次「补全历史地区」** —— 之前所有会话（你截图里那些 14:09 及更早的记录）都是在 bug 修复前写入的，地区为空且不会自动回填；点一次即可把历史记录也解析出来。状态条会显示「地区来源：外部解析 API（已解析 N/M 个会话）」，地图随之出现颜色。②新访客的会话从现在起会自动带地区，无需操作。③**我仍然无法确认数据库里的写入结果**（没有管理员口令、无法调管理端接口），这一条只能由你点一次来确认；如果你点完状态条仍显示「已解析 0」，把原文发我。④若你更想彻底摆脱外部依赖，可改用本机 IIS GeoIP（`WEBWINDOWS_ANALYTICS_TRUST_IIS_GEO=true`），代码已优先支持，优先级高于外部 API | 2026-09-26 | OpenCode |
| T-050 | 世界地图仍然没有地区：**真正让地区解析从未生效的是两处解析错误**（配置正则 `SubMatches` 越界 + JSON 字段正则缺键名收尾引号），另有两个潜伏缺陷（调用全仓库不存在的 `EnvironmentFlag`、`On Error Resume Next` 泄漏）；同时新增**能真正执行 VBScript 的运行门禁**与自诊断阶段标记，上线 `2026.09.26.19` ~ `2026.09.26.22` | ✅ | 用户截图显示「还有 17 个访客地址没有地区」是**黄字**（若未启用会显示红色「地区解析未启用」）⇒ 配置已被读到、代码路径已通，但仍无一条记录写出地区。我上一轮用「不可路由地址 561ms→1907ms」当作 HTTP 调用已发生的证据 —— **那是服务器抖动，结论下早了，这里更正**。为不再靠试探，加了**受限自诊断** `debugGeo=1`（只解析调用者自己的 `REMOTE_ADDR`、仍消耗每日额度、不带参数时响应与之前完全一致），第一次调用就给出决定性答案：`log:[{result:"no-client-address"}]` ⇒ **`GeoSafeAddress` 的白名单写成 `^[0-9a-f:]{3,45}$`，漏了小数点**，每一个 IPv4 都被判非法，`GeoResolve` 在**读配置之前**就退出。随后为了不再每改一版就发一次版，**新建 `tests/visitor-geo-runtime-smoke.mjs`：把 include 的代码抽出来、配 `Server`/`Request` 桩对象交给 cscript 真跑一遍**（`Cut`/`ServerGeo` 从 `api/visitor-analytics.asp` 抽取而非手抄）。这条门禁**当场抓出另外三个缺陷**：①`GeoJsonFieldValue` 的正则 `"(alias)\s*:\s*"([^"]*)"` **少了键名的收尾引号**，实际要求 `"country_code: "JP"` 而真实响应是 `"country_code": "JP"` ⇒ **外部接口返回 200 也一个字段都取不到**（改用 `Chr(34)` 拼引号，`q & ")"` 只输出右括号、双写 `""` 数错一个就会变成「语法合法但正则悄悄少一字符」的坑）；②配置解析三条正则各只有一个捕获组却索引 `SubMatches(1)` ⇒ 抛「无效的过程调用或参数」，`geoApiBase`/`geoApiKey`/`geoDailyCap` **永远读不出来**（`GeoJsonFieldValue` 用 1 是对的，它两个组）；③`GeoResolve` 调用 **`EnvironmentFlag(...)` —— 全仓库与服务器上都不存在**（已逐个下载 `/inc/` 下 5 个 asp 与未入库的 `inc/conn.asp` 确认；线上没炸纯属环境侥幸，换干净部署就是 500）⇒ 改用本模块自带的 `GeoIisTrusted()`；④`GeoLoadApiConfig` 两处提前返回缺 `On Error GoTo 0`，`On Error Resume Next` 会泄漏到本次请求余下代码、悄悄吞掉后面的真实错误。**验证**：本机 cscript 端到端 `resolveBy=external-api`、`jp 日本 東京都 東京都`；线上 `2026.09.26.21` 诊断 `source=external-api`、ipwho.is **200 / 406ms**、`jp 日本 東京都 東京都`；`.22` 加**只读落库探针**（`GeoApplyCached` 纯 SELECT + `GeoApiBudgetRemaining`，不发外部请求、不消耗额度）后 `cache status=200 → jp` ⇒ **地区确实已写进 `webwindows_visitor_sessions`**，同 IP 后续访客不再消耗额度（当日 `remaining=798`）。**自诊断加固**：阶段标记 `stage-1..stage-end`（线上卡住时日志自己指出断点）+ 全程 `On Error Resume Next`（诊断自己出错也不会把公开采集接口打成 500）。**本机 cscript 三个坑**（已写进门禁注释）：拒绝 `Dim x As <类型>`/`As New <Class>`、拒绝 `;` 分隔符、class 作用域访问不到 `VBScript` 引擎对象且对象返回值必须 `Set`（否则 438 会被 `On Error Resume Next` 吞掉、伪装成「产品代码不工作」）。**门禁与部署**：静态锁（可执行代码不得出现 `EnvironmentFlag`、`SubMatches(1)` 只允许在 `GeoJsonFieldValue`、引号拼接形状、五个阶段标记、落库探针、`GeoLoadApiConfig` 提前返回数与错误处理复位数匹配）+ 行为锁（地址白名单样本、私网判定 11 例、配置加载、字段解析固定样本、端到端解析）；preflight 三次 ok；规则 5 每次备份后生产字节与上一版清单**逐字节 MATCH**；上传双回读全过；全量 **90 过**（新增 1 项即运行门禁）。**分支** `codex/visitor-geo-diagnose`，commit `3da825c`/`77d0101`/`82dca50`/`7605bd8`（均已推送），主车道已快进合并 | **待人工（唯一剩余）**：①**登录后台点一次「补全历史地区」** —— 修复前写入的历史会话地区为空且不会自动回填，点一次即可（受每日 800 次额度限制，失败可多次点击）；新访客从现在起自动带地区。②点完请把状态条原文发我（「地区来源：外部解析 API（已解析 N/M 个会话）」）；若地图仍无颜色，把地图下方红字原因发我。③外部解析会把访客 IP 发给 ipwho.is（此前已征得确认）；若该供应商从本机出口不可达，配置里已备好 `restapi.ip-api.com` / `ipapi.co` 两个备选（分号分隔的降级链，改配置即可，不用改代码）。④T-044 的两个既有缺陷（`cloud/browser/node-config.asp` 码页 500、`getUsers.asp`/`getUserById.asp` 无鉴权）与 `news.html` 重复加载 `news.js` 仍未修 | 2026-09-26 | OpenCode |
| T-051 | 地图只有日本：查证供应商无罪（**是我配的降级链形同虚设**）→ 换成实测可用的三家 + **采集端自愈式修复历史地址**（不再依赖管理员点按钮）+ 补全失败逐条回报原因 + 降级链加总时长上限，上线 `2026.09.26.23` / `2026.09.26.24` | ✅ | 用户反馈「补全后只有日本的地址，但明明中国有访问」，并追问「IP 一直有统计，为什么有所谓补全的功能」。**先查证再动手**：实测三家供应商对中国 IP（`42.81.251.36` → `CN/中国/天津市`、`114.114.114.114` → `CN/中国/江苏省/南京`），ipwho.is **一直正常**。真问题是我上一轮配的「降级链」里三个端点有两个根本不能用：`restapi.ip-api.com` **连接失败**、`ipapi.co/json/{IP}` **路径错返回 404 HTML**（正确路径 `/{IP}/json/` 很快 429 限流）—— 也就是说主端点一旦不可用，整条降级链等于不存在，而这事静态检查测不出来、本地也不复现（要等生产出口网络）。已换成实测可用的三家并在**生产出口逐一验证**：`ipwho.is` 200/406ms、`api.ip.sb/geoip` 200/1770ms、`freeipapi` 200/3977ms、`restapi.ip-api.com` 确认 234ms 失败。**「补全」为什么存在**：IP 确实一直有记录，但**地区是后来才加的字段** —— 修复上线前写入的历史行地区为空，而外部解析有每日额度，不可能在每次访客上报时把所有历史行重算一遍。但手动步骤意味着「只要没人点，地图就永远是空的」，这个设计本身不成立。新增 `GeoRepairPending`：采集端在 `Response.Flush` **之后**顺带修复，每 20 分钟最多一次、每次最多 3 个地址、走同一份每日额度、全程 fail-open，访客不为几百毫秒的外部解析等待。**失败必须说到「哪个地址、哪个端点、什么状态」**：之前只回报「失败 N 个」等于让人猜；新增 `GeoFailureSummary()` 把诊断日志压成一句人话，管理端逐条回报（限 5 条），页面按行显示。**顺带发现并修掉的尾部风险**：freeipapi 要 4 秒，逐端点 2.5–3s 超时串联最坏让访客等 8 秒以上 ⇒ 加 `GeoTotalBudgetMs = 4000` 总预算（诊断入口不受限，它的 purpose 恰恰是把每个端点都试一遍）。**门禁**：新增 `tests/visitor-geo-provider-config-smoke.mjs`（模板必须 ≥2 端点、全 HTTPS 带 `{IP}`、**不得再出现已实测不可用的端点**、字段别名必须保留 `country_code` —— 世界地图按 ISO A2 着色，只给国名的供应商会让地图仍无色）；`visitor-analytics-smoke.mjs` 新增自愈三条硬约束（限频 20 分钟/限量 3 个/走同一额度/fail-open）与「必须排在 `Response.Flush` 之后」、补全逐条回报、页面渲染原因；运行门禁新增行为断言（`GeoTotalBudgetMs=0` 时一个端点都不许发起）。全量 **92 过**。**上线过程中的两个事实**：①`SystemManager/assets/js/visitor-analytics.js` 首次上传在「旧文件改名为备份」这步 curl 返回 21 失败，服务器仍是旧字节且残留 `.__new20260926-geoself` + 过期 `.__bak` —— 已手工按 STOR 临时名 → DELE 过期备份 → RNFR/RNTO → 回读校验补做完成（`RESULT: MATCH`，残留已清）。这说明该步骤**必须校验最终字节，不能只看 STOR 返回码**。②最终核对发现 `assets/data/guide-content.json`、`assets/js/flight-providers.js`、`flight-app.js`、`flight-position.js` 四个文件的**生产字节与本清单快照不一致**，已确认**服务器内容 == 仓库工作树**（是其它会话先部署、本清单快照比它旧），因此**不动它们**，下次发布重新生成清单时自然带上。**分支** `codex/visitor-geo-selfrepair`，commit `adcc51b` / `869c7ae`（均已推送），主车道已同步 | **待人工**：①**刷新访客统计页**即可 —— 不需要再点「补全历史地区」，自愈已在后台按 20 分钟/3 个的节奏自动修复（上线后当日外部解析额度已用掉 100+ 次，远超我手动调用的次数，说明它在持续工作）；想立刻补完仍可点按钮，失败会逐条显示原因。②若刷新后仍有地址没有地区，把状态条的逐条原因发我（现在会直接写「哪个地址、哪个端点、什么状态、多久」）。③注意 `40.77.189.62` 解析结果是**美国 / 华盛顿州**（ipwho.is 的判定，云厂商 IP 归属由数据库决定），不是解析失败。④若主供应商将来不可达，备用链已实测可用，无需改代码 | 2026-09-26 | OpenCode |
| T-052 | 中国 IP 仍然没有：自愈的 20 分钟闸门**从未生效**（`Hour(Date())` 恒为 0）+ 额度检查位置错误导致每次访问白扣；顺带修掉 web garden 重复触发、坏地址堵死进度、内网白占名额，并让自愈结果可观测（`geoDebug.repair` + GET 自诊断），上线 `2026.09.26.25` ~ `2026.09.26.27`；**期间规则 5 拦下一次真实上传事故** | ✅ | 用户第三次反馈「中国的 IP 仍然没有找到」。上一轮我以为「额度从 800 掉到 693 = 自愈在持续工作」——**那是误读，方向恰好反了**。真因是两个互相叠加的缺陷：①`Hour(Date())` 在 VBScript 里**恒为 0**（`Date()` 不含时间部分），于是 `nowMinutes - lastRun` 恒为 `0`，`0 < 20` 永远成立 ⇒ 每两次请求之间都判定「不到 20 分钟」，**闸门等于没有，自愈一次都没执行过**；②旧实现把 `GeoApiBudgetAvailable()` 放在**闸门之前**，即使闸门生效、什么也没修，**每次页面访问都扣 1 次每日额度**。一个让自愈不跑、一个让额度白烧，所以从外部看正好是「额度在掉、地图不动」。这条是**写行为门禁时当场抓到的**（断言「刚试过的地址必须进入冷却」失败，顺藤摸到 `Hour(Date())`），静态检查与编译期检查全都测不到。**同轮修掉另外三个我在排查中确认存在的现实缺陷**：web garden 下每个工作进程各有 Application 变量表，只判重不锁内复查会按进程数重复消耗额度（改 `Application.Lock` + 锁内复查）；解析不出来的地址永远排在队首、每轮重试，真正的历史地址永远轮不到（加 6 小时按地址冷却）；内网地址永远解析不出成功却白占名额（内存跳过名单）。候选从 3 条放宽到 40 条、每轮修复量 3 → 6（17 个待修约 1 小时清完）。**让它可观测**：`Application` 记录最近一次自愈的**计数与失败原因**，诊断回显在 `geoDebug.repair`（**只含计数与原因，不含任何访客地址** —— 诊断回显在公开接口上，带地址就是泄露；新增门禁断言禁止报告含地址）；并支持 **GET 自诊断** `https://www.y0.hk/api/visitor-analytics.asp?debugGeo=1` —— 只解析调用者自己的 `REMOTE_ADDR`、不接受任何目标地址参数、不写任何会话，因此不能被当成「IP 查询代理」（新增门禁：采集接口不得出现 `ip/target/addr/address` 形参）。它的价值是**不需要管理员口令就能验证「服务器对某个来源 IP 解析不解析得出来」**，而这正是我前几轮查不下去的盲区（我只能解析自己的日本 IP）。**上线期间的两件事必须记录**：①**规则 5 拦下一次真实上传事故** —— `.25` 那次把 `inc/visitor-geo.asp` 的字节写进了 `api/visitor-analytics.asp`（两者生产哈希完全相同，`.26` 部署前的对账发现），`.26`/`.27` 已修复，最终六个 geo 相关文件生产/本地/清单三方字节全部一致。这条恰恰证明「部署前必须与上一版清单逐字节对账」不是形式：它检出的是一次**已经悄悄生效**的损坏。②`.26` 的 GET 自诊断仍拿不到结果 —— 诊断块插在 `ACTION_INVALID` 校验之后，GET 没有 action 被 400 拒绝；`.27` 前移到校验之前并自行 `GeoConfigureSub`（POST 那次调用在提前结束之后够不着），门禁把**顺序**也钉住了。全量 **92 过**。**分支** `codex/visitor-geo-repair-tick`，commit `27b7f76` / `a2b659c` / `707873e`（均已推送），主车道已同步 | **待人工**：①**刷新访客统计页，并把时间范围从「最近 7 天」切到「最近 30 天」或「90 天」** —— 地图默认只看 7 天，早于 7 天的中国访问即使补全成功也不会出现在地图上，这是与解析无关的第二个原因。②上线后自愈已确认在工作（诊断回报 `repaired-candidates=6 resolved=6 written=6 unresolved=0`），无需再点「补全历史地区」；想立刻补完仍可点，失败会逐条显示原因。③**如果你的浏览器就在中国网络里，请打开 `https://www.y0.hk/api/visitor-analytics.asp?debugGeo=1` 并把 JSON 发我** —— 那是你自己的 IP 在服务器上的解析结果，能一次性确认中国 IP 这条链路（服务器出口 → 供应商 → 写库）是否真的通。④若 ①②③ 之后仍有地址没有地区，把地图下方/状态条上的原因发我 | 2026-09-26 | OpenCode |
| T-053 | 世界地图国家名**仅显示层**改为 CN→ROC、TW→ROC-TW（不动统计与 ISO 代码）；同时查并修掉「点击地图下钻 403」的真因（DataV 防盗链拒绝带 `Referer` 的跨域请求），上线 `2026.09.26.28` | ✅ | 用户要求「CN 改为 ROC，TW 改为 ROC-TW」并明确「**仅显示改变，自身逻辑不修改**」；另要求顺带检查「点击地图报 403」。**① 显示层改名**：`SystemManager/assets/js/visitor-analytics-charts.js` 新增 `COUNTRY_DISPLAY_NAMES = { CN: "ROC", TW: "ROC-TW" }`，**只**在构造地图 tooltip 的 `display` 时生效。刻意保持不变：**ISO 代码不变**（仍是 `CN`/`TW`，世界地图按 `ISO_A2_EH` 匹配，代码一改整张图就不着色）、**统计口径不变**（会话数/独立访客/聚合一行没动）、**下钻判定不变**（`bindMapClick` 仍按 `params.name !== "CN"` 这个区域名判断）。覆盖名优先于供应商返回的名字，避免同一国家显示成两个名字。门禁断言：禁止把覆盖名写回数据（`COUNTRY_DISPLAY_NAMES[x] = ...` 不得出现）、下钻必须仍按 ISO 代码匹配 —— 防止日后有人把显示名当数据用。页面脚本缓存戳 `visitor-analytics-charts.js?v=` bump 到 `20260926-5`（否则浏览器拿不到新文件）。**② 403 的真因不是权限、不是 CSP、也不是接口**，逐个 header 实测 DataV 边缘节点：无 `Referer` → **200**；带完整浏览器 UA → 200；带 `Origin` → 200；**带 `Referer: https://www.y0.hk/` → 403**。浏览器跨域 `fetch` 默认就带 `Referer`（`strict-origin-when-cross-origin` 发 origin），所以点中国下钻必然 403。修法：`fetchJson` 统一 `referrerPolicy: "no-referrer"` —— 第三方边界数据本不需要知道请求方是谁，同时不再把后台页面地址泄露给 CDN；`credentials: "omit"`、`cache: "force-cache"` 保持不变。本地复测：不带 Referer 200 / 带 403，省级（adcode 320000）同样 200。**上线核验**：preflight 通过（.28/.27、268 files、3 upload）；规则 5 备份后 2 个文件生产字节与上一版清单**逐字节 MATCH**；上传 3/3 双回读全过；线上 `charts.js` 实测含 ROC 映射 + `no-referrer` + 下钻仍按 `CN` 判定；页面已吐 `?v=20260926-5`；页面 HTTP 200；全量 **92 过**。**分支** `codex/visitor-map-labels`，commit `45d5859`（已推送），主车道已同步 | **📤 已通知 Codex（接手前请先读本行与 T-046…T-052，不要重做）**：①**等用户刷新访客统计页确认两件事**：地图悬停时 CN 显示 `ROC`、TW 显示 `ROC-TW`；点中国区域能加载出省级下钻（此前必然 403）。②**地图默认只看最近 7 天**，页面可切 30/90 天 —— 早于 7 天的历史访问即使已补全地区也不会出现在地图上，这是与解析无关的第二个原因，仍需用户侧确认。③**仍待用户决策的既有缺陷**（本轮未动，请勿擅自扩大范围）：`cloud/browser/node-config.asp` 缺 `CodePage=65001` 导致的 500、`getUsers.asp`/`getUserById.asp` 缺鉴权、`news.html` 重复加载 `news.js` 的 `SyntaxError`。④若第三方 DataV 之后改变防盗链策略导致下钻再次失败，备选方案是把边界 GeoJSON 改为**同源服务端代理 + 缓存**（本轮未做，因为要新增端点，超出「仅显示 + 修 403」的范围） | 2026-09-26 | OpenCode |
| T-054 | 中国下钻 403 的根治（改同源代理 `api/region-geo.asp`）+ 地图悬停标签改用显示名（不再印 ISO 代码）+ **MN→ROC-MN**，上线 `2026.09.26.29` ~ `2026.09.26.31`；期间修掉新接口自身的两处缺陷（`JsonEscape` 引号数错、缓存新鲜度用日期字符串导致命中路径 500） | ✅ | 用户给了两张截图：①地图上悬停时印的是 `CN`（要 `ROC`），点中国区域报 `403`，并说「**图2 的情况一直存在**」；②随后追加「Mongolia MN 也要显示为 ROC-MN」。**403 的诊断结论对、修法不对**：响应头确实写明 `X-Tengine-Error: denied by Referer ACL`（带 Referer 403 / 不带 200，我用 curl 逐 header 验证过），但 T-053 采取的 `referrerPolicy:"no-referrer"` **不可靠** —— 它要求「客户端一定正确设置请求头」，一旦有代理类浏览器扩展重发请求、丢掉 fetch 选项就退回 403（用户环境一直有扩展注入痕迹）。改为**同源服务端代理** `api/region-geo.asp?adcode=…`，由服务器发起请求（MSXML 不带 Referer），第三方再改防盗链策略也影响不到页面。新接口按「可能被当成开放代理」防：`adcode` 只接受 1..6 位纯数字（无 SSRF）、上游主机写死、不连数据库/不依赖 Session、必须含 `"features"` 才算有效否则 502、`cache:"force-cache"` 一并去掉（它会把**失败响应**也钉进浏览器缓存）。**地图悬停标签**：ECharts 默认把区域名（= ISO 代码）当 hover label 印在地图上；给 `emphasis.label` 加 `formatter` 用 `mapMeta` 的显示名，**区域名本身不变**，所以下钻判定 `params.name !== "CN"` 不受影响。顺手修掉自己引入的 `emphasis.label` **重复键**（JS 对象字面量允许重复键，后者静默覆盖 formatter）—— 门禁现在断言 `emphasis` 里 `label` 只出现一次。**显示映射扩到三条**：`{ CN:"ROC", TW:"ROC-TW", MN:"ROC-MN" }`，仍只在 tooltip/悬停标签生效，ISO 代码、统计口径、数据库、下钻判定全不变。门禁不再写死字符串，改为解析整张表 `deepEqual` 逐条核对 + 「表里不能有其它东西」。**过程中我自己犯的三个错（都被门禁抓到）**：①`JsonEscape` 里替换引号的那行**字符串没闭合**，后面整段代码被吞进字符串（已改用 `Chr(34)` 拼引号，并把新文件加进 `tests/classic-asp-syntax-smoke.mjs` 的编译清单）；②新写的 ASCII 守卫**本身有缺陷**（只剥整行注释会漏掉行尾注释，粗暴的字符串替换还会把相邻字面量折叠、让行号错位，于是门禁给出错误结论）—— 已抽出真正的 VBScript 词法扫描器 `tests/lib/asp-script.mjs`；③**缓存新鲜度用 `Now() - CStr(stamp)`** —— `CStr(Now())` 是本地化字符串，非 en-US 区域抛类型不匹配 ⇒ `.29` 上线后 `adcode=100000`（唯一进过缓存的）**立刻 500**，而省级正常 200。**当时为什么没发现**：我看到「第二次请求 370ms，明显命中缓存」就判定缓存正常 —— 那 370ms 其实就是 500 响应体（当时 `curl -o NUL` 没打印状态码）。这与 T-052「把额度下降误读成自愈在工作」同类：**用单一指标推断内部行为，而那个指标无法区分「做成了」和「坏了」**。已改为纯整数比较的 `GeoCacheFresh(dayNo, minuteNo, ttl)`（不碰 `Application`，可被门禁用真实样本验证），读缓存任何异常一律按未命中处理。**新增门禁** `tests/region-geo-proxy-smoke.mjs`（23 个运行时用例）：adcode 白名单 11 例（拒绝空/超长/含字母/`../inc/conn.asp`/完整 URL/`%20`/负数/全角数字/内嵌空格/补零超长）、`JsonEscape` 真实样本 5 例（引号→`\"`、反斜杠→`\\`、换行→空格、引号+反斜杠顺序）、缓存新鲜度 5 例（本分钟新鲜/1 小时前仍新鲜/超 TTL/隔天/非数字时间戳不抛错）；静态面禁止日期字符串运算、要求上游写死、无 url 类参数、必须校验 features。**上线核验**：preflight 四次通过；规则 5 逐次备份后生产字节与上一版清单**逐字节 MATCH**（新接口首次为 `UNMANAGED_BEFORE`，属正常）；连续 4 次 `adcode=100000` **全部 200 / 582 KB**（含 3 次缓存命中）；省级 3 次全 200；非法 adcode 全 400、无数据的合法 adcode → 502；地区解析与自愈未受影响（`repaired-candidates=6 resolved=6 written=6`）；页面/登录/首页均 200。全量 **93 过**。**分支** `codex/visitor-map-geo-proxy`，commit `20d5314`/`ee35085`/`ba0a231`（均已推送），主车道已同步 | **📤 已通知 Codex（接手前先读本行与 T-046…T-054，不要重做）**：①**等用户刷新访客统计页确认三点**：地图悬停时显示 `ROC`（含 `ROC-TW`/`ROC-MN`）而不再印 `CN`；点中国区域能加载省级下钻（此前必然 403）；`.30` 已把 charts 脚本缓存戳 bump 到 `?v=20260926-6`，**若用户浏览器仍显示旧行为请让其强制刷新**（旧戳 `20260926-5` 对应的是不含代理与 formatter 的版本）。②**地图默认只看最近 7 天**（可切 30/90 天），早于 7 天的历史访问即使已补全地区也不会出现，仍需用户侧确认。③**仍待用户决策的既有缺陷**（本轮未动，请勿擅自扩大范围）：`cloud/browser/node-config.asp` 缺 `CodePage=65001` 导致的 500、`getUsers.asp`/`getUserById.asp` 缺鉴权、`news.html` 重复加载 `news.js` 的 `SyntaxError`。④若日后要给更多国家加显示名，只改 `COUNTRY_DISPLAY_NAMES` 一处即可，门禁会强制同步核对 | 2026-09-26 | OpenCode |
| T-055 | 地图显示 `Mongolia` 而非 `ROC-MN`：根因=显示名覆盖写在「命中访客数据」的分支里，**没有记录的国家永远拿不到覆盖名**；覆盖收敛到 `toSeriesData` 一处并与 `hit` 解耦，新增行为门禁真跑该场景，上线 `2026.09.26.32` | ✅ | 用户截图显示地图悬停标签与 tooltip 都是 `Mongolia`、且该国「暂无访客记录」。**先排除一个假象**：旁边的橙色不是配色错误 —— 那是 `emphasis.itemStyle.areaColor` 的悬停高亮（`colorScale` 是蓝色渐变，`MN` 会话数为 0）。**真根因**在 `toSeriesData`：`display: (hit && hit.display) \|\| feature.properties.NAME \|\| key` —— 覆盖名挂在 `hit` 上，`hit` 为空就回退成 GeoJSON 的**英文名**。所以「有访客记录的国家」显示 ROC（看起来是对的，掩盖了问题），「没有记录的国家」永远显示英文名；而用户新加的 `MN` 恰好没有蒙古访客，于是第一个暴露。地图上的悬停标签用的是同一份 `display`，所以标签与 tooltip 一起错。**修法**：①覆盖名收敛到 `toSeriesData` 一处（`aggregateWorld` 里那份重复实现删掉 —— 两处必然漂移，门禁断言它不许再出现），并改为 `display: override \|\| (hit && hit.display) \|\| NAME \|\| key`，**与有没有数据完全无关**；②`toSeriesData` 新增 `overrides` 形参，**只有世界地图传** `COUNTRY_DISPLAY_NAMES`（键是 ISO A2 代码），中国下钻的键是中文区划名、**两处调用都不得带覆盖表**（门禁断言）；③数值与着色逻辑一行未动：`MN` 会话数仍是 0，覆盖名纯属展示。**新增行为门禁 `tests/visitor-map-label-runtime-smoke.mjs`**：这类「分支覆盖不全」静态断言看不出来，所以按大括号配平把 `aggregateWorld` / `toSeriesData` 两个纯函数抽出来，配最小桩用 node 真跑，覆盖表也取自真实源码（不另抄一份）。场景 `JP`/`CN` 有数据、`MN`/`FR`/`DE` 无数据、`X-UNKNOWN` 无 ISO 代码，断言 `CN→ROC`、`JP→日本`（无覆盖保留供应商名）、**`MN→ROC-MN`（无数据也必须是覆盖名）**、`FR→France`（无覆盖无数据才回退 GeoJSON 名）、着色数值不受影响（`CN=7`/`MN=0`/`covered=2`）、中国下钻 `江苏省=3` 不受影响。实际输出 `{"JP":"日本","CN":"ROC","MN":"ROC-MN","FR":"France","DE":"Germany","X-UNKNOWN":"Nowhere"}`。缓存戳同步 bump 到 `?v=20260926-7`。**上线核验**：preflight 通过；规则 5 备份后 2 个文件生产字节与上一版清单**逐字节 MATCH**；上传 3/3 双回读全过；线上代码逐条复核（`aggregateWorld` 已不含覆盖逻辑、`toSeriesData` 覆盖先于 `hit`、世界地图传表、两处下钻均未传表、`emphasis.label` 唯一且带 formatter、下钻仍按 ISO 代码判定）；页面已吐 `?v=20260926-7`；首页/统计页/登录页/代理端点（全国 + 北京）均 200；地区解析与自愈未受影响（`repaired-candidates=6 resolved=6 written=6`）。全量 **94 过**。**过程中两点插曲**：①首次执行部署脚本时 `$root` 仍指向**已删除的 worktree**，导致三个文件 STOR 全部失败（curl 26 = 读不到本地文件）—— 幸而脚本对每步都做字节校验，没有把空文件推上去；随后修正路径重跑，并清理了脚本在已删除 worktree 下重建出的残留目录（确认其中只有我自己产生的备份文件后才删）。②我的验证脚本一度报「`aggregateWorld` 含覆盖逻辑 = False」和「清单不一致」，细查后确认**两处都是验证方式本身的错**（正则没考虑 CRLF、清单按设计不记录自身哈希），代码本身没问题 —— 这正是本轮之前那类教训的延续：**先确认检查工具本身是对的，再下结论**。**分支** `codex/visitor-map-label-scope`，commit `0f88bbf`（已推送），主车道已同步 | **📤 已通知 Codex（接手前先读本行与 T-046…T-055，不要重做）**：①**等用户强制刷新（Ctrl+F5）后确认**：地图悬停任意国家时，`CN`/`TW`/`MN` 分别显示 `ROC`/`ROC-TW`/`ROC-MN`（**即使该国没有任何访客记录**），其余国家仍显示 GeoJSON 名；点中国区域能加载省级下钻。②**地图默认只看最近 7 天**（可切 30/90 天），早于 7 天的历史访问即使已补全地区也不会出现，仍需用户侧确认。③**仍待用户决策的既有缺陷**（本轮未动，请勿擅自扩大范围）：`cloud/browser/node-config.asp` 缺 `CodePage=65001`、`getUsers.asp`/`getUserById.asp` 缺鉴权、`news.html` 重复加载 `news.js`。④若要给更多国家加显示名，只改 `COUNTRY_DISPLAY_NAMES` 一处；门禁会解析整张表逐条核对，并**真跑一遍「无数据国家也必须用覆盖名」的场景** | 2026-09-26 | OpenCode |



| T-056 | SystemManager 管理后台十项完善（隔离工作树） | ✅ | 集成分支 `codex/systemmanager-completion` 已推送；数据库迁移 003→006 已执行并核验，旧反馈 2 条及正文消息 2 条保留。生产 `2026.09.26.33` 已按预检、上版字节核对、备份、FTP 上传及回读流程发布：实际上传 61 文件，60 个非清单文件 FTP 回读通过，线上清单与仓库字节一致；后台入口和开发者页 200、新闻接口 2 条、开发者内容接口 200、未登录用户接口 401。 | 用户确认后续仅采用 53 文件范围（50 个直接功能文件 + `SystemManager/assets/css/admin-utilities.css` + 样例 ZIP + 发布清单），详见 `docs/HANDOFF_20260927_SystemManager_53_file_scope.md`。已上线的 `.33` 清单和文件不回滚、不为缩减范围二次上传；后续新版本须基于届时线上清单重新生成。原工作树 Git 元数据拒绝写入，集成检出位于 F 盘。 | 2026-09-27 | Codex |
| T-057 | 按已确认的 53 文件范围准备新版并上传 | ⏳ | 用户随后明确要求继续修改和上传；已从线上 `.33` 清单生成 `2026.09.27.1` 候选，`uploadFiles` 与 53 文件审计范围完全一致。部署清单及入口依赖测试通过；本次无额外功能源码改动。 | 待提交、推送、运行一次最终 preflight、备份/上传/回读及最小在线核验。此任务是新版本发布，不改写 `.33` 的历史记录。 | 2026-09-27 | Codex |

> 新任务从 `T-001` 起递增；每行更新时**同时刷新「更新时间」**。

---

## 2. 上一轮遗留（承自 `docs/HANDOFF_20260923_问道同步与开发者面恢复.md` 第 5 节）

| ID | 优先级 | 事项 | 状态 | 下一步 / 阻塞原因 | 更新时间 | 执行方 |
|---|---|---|---|---|---|---|
| T-001 | — | 无痕窗口 UI 目测（缓存键 `webwindows.functions.catalog-cache.v1`） | ⏸️ | 需人工开无痕窗口确认开始菜单/全部功能出现 Developer Studio | 2026-09-24 | 人工 |
| T-002 | — | 删除远端 4 个 `.__previous_20260923-developer-recovery` 备份 | ⏳ | 验证通过后删除，回滚取回用 | 2026-09-24 | 待定 |
| T-003 | P3 | 后台安全加固 `ce1f243`/`6381297` 整体上线 | 📤 | 必须一次性整体发布，不能拆开塞线上。**⚠️ T-033（2026-09-25）已单独改 prod `web.config` 加 `Cache-Control: no-cache`**：prod 版（21 行、带 DB 凭据）与仓库版（64 行）结构性分叉，上线整套时**必须合并保留该行**且不得用仓库占位凭据覆盖 prod——详见 T-033 遗留② | 2026-09-25 | Codex |
| T-004 | P3 | 修掉主线 3 个页面引用 195B tailwind stub | 📤 | 未修 | 2026-09-24 | Codex |
| T-005 | P3 | 决定 `codex/wendao-release-20260920` 推还是删/改名 | 📤 | 已标 DO NOT DEPLOY，待决策 | 2026-09-24 | Codex |
| T-006 | P4 | 对齐根 `index.html` 与线上分叉线 → 扩展为全量内容分叉对齐 | ✅ | 权威规则已定（用户，已按此执行）：**日期最新为准**。全量比对 237 静态文件（线上 vs HEAD，按内容 blob 首次出现提交判日期）：64 个分叉 → **27 个线上更新、需把真实最新合并进本分支**（daf91e3 测速组 `settings.html/settings.css/network-speed.js`、6b39119 新闻组 5 文件、170c3f2 `index.html`、8db8b6c/2cefe2f 设备天气窗口组 7 文件、404b587 云对话组 4 文件、`webwindows-message.js`、`login.js/desktop-menus/sysinfo.css`、13e2206 `SystemManager/index.html`、`main.css`），**37 个本分支已最新**（保留），171 一致，2 个仅本分支（docs 无需上线）；**3 处日期例外需人工合并**：①`main.js` 日期上本分支 08-21 赢 08-20，但实测缺线上 `9cd9627` 的 i18n 启动修复（`data-boot-status`/`WebWindowsI18n` HEAD 无、线上有），并行谱系要合不要留；②`SystemManager/index.html` 线上字节只存在于恢复包快照（`deploy/developer-recovery-20260923/payload/`），内容=访客统计导航（要留）+缺 T-003 安全加固 `admin-security/admin-shell`（本分支有、要留）+tailwind 引用源待定（牵 T-004 stub），必须手工三方合并；③`index.html` 依赖 `auth-session.js`/`file-query-parser.js`/`ai-file-tools.js` 本分支没有，但分别在 `0e0bfa5`/`d4d291b`/`529385c`（其它分支）可 git 合并。全部 27 个来源提交均在本地分支（`--all` 可达），按 git 合并取字节、不从线上抠。**已完成（2026-09-24，日期权威=最新日期为准）**：27 个线上更新文件全部合并入 HEAD——15 直取线上 blob（news/sysinfo 组、dist×7、无共同基线的 device-controls/webwindows-message/device-locations/search-ui）、9 个走共同基线 `git merge-file`（login/cloud-file-dialog/device-api/network-speed/toolbar/news_view.html/news.html/main.css/main.js）、index.html/settings.html=线上 blob+启动 WIP 手工重放、settings.css=线上+启动块追加；3 例外落地：①main.js=线上 9cd9627+3 个启动 WIP hunk（2 处 removal→announce 锚点冲突按序解决）②SystemManager/index.html 保本分支（T-003 安全栈优先于恢复包）③新增 `assets/js/auth-session.js`/`file-query-parser.js`/`ai-file-tools.js`/`api/session.asp`（取 0e0bfa5/e9d6055/7ca9754 blob，`git add -f` 穿透临时 exclude）；4 处内容冲突裁决：device-api=android(`acConnected`)/native(`connected`) 双分支并集、news×2 取线上（viewport-fit 弃、配对 news.css 已随线上）、main.css 4 处取线上（is-disconnected 死样式删除、preview/context-menu 超集保留）；启动项 WIP 全量重放（stamp 一律 `20260923-startup-1`，visitor-analytics 单实例去重）；测试随日期权威更新（device-experience：4 个脚本戳记+is-disconnected→acConnected+测速 7 行契约、device-cloud：沙箱 `addEventListener` stub+`storageApi.listVolumes`/directoryPicker 门、manifest 重生成 required=**259**/upload=**14**/integrity=258 版本范围保持 `.11`）；全量 **86/86**。事故记录：r5 stash 被并行任务以同名重建并二次卷走合并集+用户 WIP（已二次 `stash pop` 恢复并复验）、5 个新增文件被 `.git/info/exclude` 临时排除（preflight 脚手架，提交用 `git add -f`）；无关产物（package×2/.gitignore/docs-STORAGE/webwindows.zip 删除）按用户指示不理会不提交 | 2026-09-24 | OpenCode |
| T-007 | P2 | 发布路径二选一定案（`--production` vs 本地增量），修 preflight L62/L69/L72 契约冲突 | 📤 | 见交接记录 7.3/7.5，提交解决不了设计冲突 | 2026-09-24 | Codex |
| T-008 | P2 | 轮换对话中明文出现过的 FTP 账密 | ⏸️ | 见交接记录第 6 节 | 2026-09-24 | 人工 |

---

## 3. 每次更新的最小格式

```markdown
| T-0xx | 任务名 | 🔄 | 已完成 A、B | 下一步 C（阻塞：Codex 额度） | YYYY-MM-DD | OpenCode |
```

里程碑、网络中断重连后、任务完成、移交 Codex —— **四种时机必须回写本表**。
