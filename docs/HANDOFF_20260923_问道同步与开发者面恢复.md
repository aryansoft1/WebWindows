# 交接留痕 · 2026-09-23 问道同步与开发者面恢复

> **本文是写给 Codex 的操作交接记录。**
> 执行方：OpenCode 会话 · 执行分支：`codex/WebWindows-开发者工具`
> 完整根因分析见 `docs/PRODUCTION_DRIFT_20260923.md`，部署细节见 `deploy/developer-recovery-20260923/README.md`。

---

## 1. 本次会话产出的提交

| 提交 | 内容 |
|---|---|
| `276f5a0` | **P0** 问道同步（24 个文件）：`road.html`、导航/路线/出行 CSS+JS+SVG、`tw.js` i18n、`desktalk.js` 的 `askTravelAdvice`、`docs/NAVIGATION_PROVIDER_V1.md`、`docs/guide-v2/21-navigation.md`、`assets/guide/navigation-flow.svg`、重新生成 `assets/data/guide-content.json`、`system-apps.json` 17 项 / `2026.09.23.1`，外加 4 个测试更新 |
| `38ae6bc` | **P2** 修正 `tests/developer-studio-deterministic-build-smoke.mjs` 里钉死的 ZIP 哈希（`fbcd4a5f…` → `48c44241…`） |
| `13e2206` | **P1 恢复包 + P4 漂移报告**：`deploy/developer-recovery-20260923/`、`docs/PRODUCTION_DRIFT_20260923.md` |
| `9e53cb5` | **P1 执行留痕**：新增 `deploy-ftp.ps1`、`manifest.json` 增 `localSource`/`optional`、README 与漂移报告回填执行结果 |

另有一个提交在**另一个分支**上：`f1e1222`（`codex/wendao-release-20260920`）—— 在该分支根目录加了 `DEPLOYMENT-UNSUPPORTED.md`。

全量测试入口：**`node --test "tests/*.mjs"`（注意：`package.json` 里没有 `test` script）**，当前 **81/81 通过**。

---

## 2. 生产环境实际被改了什么（务必读完）

### 2.1 上传了 4 个文件（2026-09-23，HTTP 回读 SHA-256 全部核对通过）

| 远端路径 | SHA-256 |
|---|---|
| `SystemManager/index.html` | `da7874db647628e79a24b938101d2474f7e0fd8e9349c3d758bc4b2613399d63` |
| `data/apps/system-apps.json` | `e42f06e2fc57f14e7202505e84d50a156fefad4efe281a4dd1b02e6f6ed80304` |
| `assets/data/guide-content.json` | `b1e902376803a5283b61671a4055e0dad2901b816140ea79a4b1f6c7615c10b2` |
| `assets/js/tw.js` | `07273e8e2a9898f143872e0728483bf1b54139928490440b705dc211a6a21240` |

远端旧版被保留为 **`<path>.__previous_20260923-developer-recovery`**（4 个，验证无误后可删，回滚直接取回即可）。

上传走 `deploy/developer-recovery-20260923/deploy-ftp.ps1`：先核对本地 SHA-256 → 旧版挪到 `.__previous_` → 传临时路径 → `RNFR/RNTO` 原子替换 → 从 `https://www.y0.hk` 回读再比对一次。凭据只通过环境变量注入单次进程，**不写入任何仓库文件**。

### 2.2 数据库：**没有执行任何 SQL**

线上 `api/function-catalog.asp` 与仓库里的版本**不一样**，它多一条 `json-upgrade` 路径（仓库版只有 `database` / `json-fallback` / `unavailable`）。上传后首次访问即返回 `X-WebWindows-Catalog-Source: json-upgrade` 并把新目录**写回 DB**，随后连打三次稳定为：

```
source=database   apps=17   developerStudio=true   version=2026.09.23.1
```

所以 README 第 3 节方式 A 的 `UPDATE … SET is_active=0` 和方式 B 的后台手工补条目，**本次都没有执行、也不需要**。README 已回填这个结论。

> ⚠️ 推论：**仓库里的 `api/function-catalog.asp` 也落后于线上**，下次改动这个文件前请先从生产取回真实版本对比。

### 2.3 验证结果（39 项 PASS）

- 后台首页两个导航链接已恢复：`functions.html`、`developer-platform.html`；仍走 CDN Tailwind，**没有**引用 195 字节 stub
- 使用向导 `registryVersion 2026.09.23.1`
- 目录 17 项的 **entry + icon 共 34 个 URL 全部 200**
- Developer Studio：`desktop=true / startMenu=true / allFunctions=true`，其 `developer-studio.html`、`developer-center.js`、`developer-studio.css`、`code.svg` 全 200
- 后台 5 个依赖文件全 200，且没有任何一个引用线上 404 的 `admin-security.js`

**未做**：浏览器 UI 目测。执行会话的桌面浏览器未连接，需要人工开无痕窗口（缓存键 `webwindows.functions.catalog-cache.v1`）确认开始菜单/全部功能里出现 Developer Studio。

---

## 3. 根因（一句话）

生产是由 `codex/wendao-release-20260920` 的一个 **subset checkout** 部署的：该分支本身就没有开发者面（缺 `developer.html`、`SystemManager/functions.html`、`developer_platform*`、21 个 `data/sdk/*` 契约、`inc/trust-schema.asp` 等 268 个文件），却多了 `admin_api/adminGuard.asp`、`assets/js/auth-session.js` 等 59 个另一条线的东西。它**不是**当前分支的子集，两条线已经分叉（973 vs 764 文件）。

---

## 4. 给 Codex 的红线（照做，别踩）

1. **不要**把仓库版 `SystemManager/index.html` 上传/覆盖到线上。它引用 195 字节的 `assets/css/tailwind.min.css` stub（会让后台首页彻底失去样式），并依赖线上不存在的 `SystemManager/assets/js/admin-security.js` 与 `csrfToken` 后端（会让后台**无限跳转 `login.html`**）。线上安全的版本是 `deploy/developer-recovery-20260923/payload/SystemManager/index.html`。
2. **不要**把 `codex/wendao-release-20260920` 当部署源。该分支根目录的 `DEPLOYMENT-UNSUPPORTED.md` 就是为这件事放的。
3. 后台安全加固 `ce1f243` + `6381297`（强制后台请求来源 / 变更要求 CSRF 证明）**从未部署到生产**。线上 `admin_api/adminAuth.asp?action=status` 返回体里没有 `csrfToken`。要上必须**一次性整体发布**，不能拆开往线上塞。
4. 主线 `index.html` 与线上是**分叉**的（线上多了 3 个只存在于另一条线的文件 `auth-session.js` / `file-query-parser.js` / `ai-file-tools.js`，以及 16 个更新的 `?v=`）。本次只做分析、**没有改动**，要对齐得先确认哪边是权威。
5. 以后凡是改线上文件，一律走 `deploy-ftp.ps1` + `manifest.json` 的 SHA-256 双向校验，别直接覆盖。

---

## 5. 还没做的事

| 优先级 | 事项 | 状态 |
|---|---|---|
| — | 无痕窗口 UI 目测 | ⏳ 需人工 |
| — | 删除远端 4 个 `.__previous_20260923-developer-recovery` 备份 | ⏳ 验证后 |
| — | `data/deploy/production-rehearsal-manifest-v1.json` 提交 | ⏳ **故意未提交**，必须和 `tools/build-production-rehearsal-manifest.mjs`、visitor-analytics 那批改动一起提交 |
| P3 | 后台安全加固 `ce1f243`/`6381297` 整体上线 | ⏳ 单独一次发布 |
| P3 | 修掉主线 3 个页面引用 195B tailwind stub | ⏳ 未修 |
| P3 | 决定 `codex/wendao-release-20260920` 推还是删/改名 | ⏳ 标记已推 |
| P4 | 对齐根 `index.html` 与线上分叉线 | ⏳ 仅分析 |

---

## 6. 安全提醒

本次执行用到的 **FTP 账密已在对话记录中明文出现，请轮换**。仓库里 `tmp/deploy-studio-storage-hotfix.ps1` 等历史脚本也保留了同样的 FTP 主机/根路径信息，属既有情况，但**不要**再把凭据写进任何会被提交的文件。
