# Hello WebWindows

这是 WebWindows 官方最小功能包样例。

1. 直接打开 `index.html` 预览。
2. 修改 `manifest.json` 中的 `id`、名称和版本。
3. 保持 `manifest.json`、`index.html`、`icon.svg` 位于 ZIP 根目录。
4. 压缩后上传到私人云资料，再从开发者中心提交。

当前 `browser-zip-sandbox-v1` 不允许外部网络、同源权限或 ES Module，因此样例无外部依赖。