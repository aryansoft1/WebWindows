# 显示

“设置 → 显示设置”包含界面缩放、当前分辨率和亮度。

普通浏览器中的亮度是 WebWindows 页面视觉亮度，通过界面滤镜实现，不改变显示器或手机背光。Android Host 若提供原生亮度能力，可能控制宿主亮度；以当前页面的能力说明为准。

界面缩放用于改变 WebWindows 内容比例。分辨率信息来自当前屏幕 API，浏览器缩放和设备像素比可能使显示值与硬件标称值不同。

