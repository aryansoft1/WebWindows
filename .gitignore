node_modules/
dist-weather/

# Local server-side secrets
.env
.env.*
!.env.example
cloud/desktalk/chatproxy.config.asp
!cloud/desktalk/chatproxy.config.example.asp
# 代理层的真实密钥配置：只提交 .example 模板
api/*.config.asp
!api/*.config.example.asp

# Local workspaces, caches, and logs
tmp/
outputs/
.cache/
.codex/
.agents/
logs/
*.log
*.tmp

# Local git worktrees of parallel agent sessions
.worktrees/
.codex-worktrees/

# Local deployment backups (may contain production copies incl. credentials)
.deployment-backups/
deploy/production-backups/

# Generated build output (existing tracked dist files remain tracked)
build/
**/bin/
**/obj/
*.dll
*.pdb

# Local archives, backups, patches, and rendered media
*.zip
*.bak
*.backup
*.patch
patch.diff
*.mp4
*.mov
*.webm

# Playwright
test-results/
playwright-report/
blob-report/
