# Deployment evidence

This directory contains machine-readable, hash-pinned deployment evidence. It is not a deployment instruction or authorization. Regenerate `production-rehearsal-manifest-v1.json` with `npm run build:production-rehearsal-manifest` from the approved release-candidate checkout, then verify it against the target host before rollout.
