# 交接留痕 · 2026-09-23 问道同步与开发者面恢复

> **本文是写给 Codex 的操作交接记录。**
> 执行方：OpenCode 会话 · 执行分支：`codex/WebWindows-开发者工具`
> 完整根因分析见 `docs/PRODUCTION_DRIFT_20260923.md`，部署细节见 `deploy/developer-recovery-20260923/README.md`。

---

## 1. 本次会话产出的提交

| 提交 | 内容 |
|---|---|
| `276f5a0` | **P0** 问道同步（24 个文件）：`road.html`、导航/路线/出行 CSS+JS+SVG、`tw.js` i18n、`desktalk.js` 的 `askTravelAdvice`、`docs/NAVIGATION_PROVIDER_V1.md`、`docs/guide-v2/21-navigation.md`、`assets/guide/navigation-flow.svg`、重新生成 `assets/data/guide-content.json`、`system-apps.json` 17 项 / `2026.09.23.1`，外加 4 个测试更新 |
| `38ae6bc` | **P2** 修正 `tests/developer-studio-deterministic-build-smoke.mjs` 里钉死的 ZIP 哈希（`fbcd4a5f…` → `48c44241…`） |
| `13e2206` | **P1 恢复包 + P4 漂移报告**：`deploy/developer-recovery-20260923/`、`docs/PRODUCTION_DRIFT_20260923.md` |
| `9e53cb5` | **P1 执行留痕**：新增 `deploy-ftp.ps1`、`manifest.json` 增 `localSource`/`optional`、README 与漂移报告回填执行结果 |

另有一个提交在**另一个分支**上：`f1e1222`（`codex/wendao-release-20260920`）—— 在该分支根目录加了 `DEPLOYMENT-UNSUPPORTED.md`。

全量测试入口：**`node --test "tests/*.mjs"`（注意：`package.json` 里没有 `test` script）**，当前 **84/84 通过**（见第 7 节：这个数字被本地未跟踪文件与 WIP 撑着，**干净检出只有 79/83**，别拿它当部署依据）。

---

## 2. 生产环境实际被改了什么（务必读完）

### 2.1 上传了 4 个文件（2026-09-23，HTTP 回读 SHA-256 全部核对通过）

| 远端路径 | SHA-256 |
|---|---|
| `SystemManager/index.html` | `da7874db647628e79a24b938101d2474f7e0fd8e9349c3d758bc4b2613399d63` |
| `data/apps/system-apps.json` | `e42f06e2fc57f14e7202505e84d50a156fefad4efe281a4dd1b02e6f6ed80304` |
| `assets/data/guide-content.json` | `b1e902376803a5283b61671a4055e0dad2901b816140ea79a4b1f6c7615c10b2` |
| `assets/js/tw.js` | `07273e8e2a9898f143872e0728483bf1b54139928490440b705dc211a6a21240` |

远端旧版被保留为 **`<path>.__previous_20260923-developer-recovery`**（4 个，验证无误后可删，回滚直接取回即可）。

上传走 `deploy/developer-recovery-20260923/deploy-ftp.ps1`：先核对本地 SHA-256 → 旧版挪到 `.__previous_` → 传临时路径 → `RNFR/RNTO` 原子替换 → 从 `https://www.y0.hk` 回读再比对一次。凭据只通过环境变量注入单次进程，**不写入任何仓库文件**。

### 2.2 数据库：**没有执行任何 SQL**

线上 `api/function-catalog.asp` 与仓库里的版本**不一样**，它多一条 `json-upgrade` 路径（仓库版只有 `database` / `json-fallback` / `unavailable`）。上传后首次访问即返回 `X-WebWindows-Catalog-Source: json-upgrade` 并把新目录**写回 DB**，随后连打三次稳定为：

```
source=database   apps=17   developerStudio=true   version=2026.09.23.1
```

所以 README 第 3 节方式 A 的 `UPDATE … SET is_active=0` 和方式 B 的后台手工补条目，**本次都没有执行、也不需要**。README 已回填这个结论。

> ⚠️ 推论：**仓库里的 `api/function-catalog.asp` 也落后于线上**，下次改动这个文件前请先从生产取回真实版本对比。

### 2.3 验证结果（39 项 PASS）

- 后台首页两个导航链接已恢复：`functions.html`、`developer-platform.html`；仍走 CDN Tailwind，**没有**引用 195 字节 stub
- 使用向导 `registryVersion 2026.09.23.1`
- 目录 17 项的 **entry + icon 共 34 个 URL 全部 200**
- Developer Studio：`desktop=true / startMenu=true / allFunctions=true`，其 `developer-studio.html`、`developer-center.js`、`developer-studio.css`、`code.svg` 全 200
- 后台 5 个依赖文件全 200，且没有任何一个引用线上 404 的 `admin-security.js`

**未做**：浏览器 UI 目测。执行会话的桌面浏览器未连接，需要人工开无痕窗口（缓存键 `webwindows.functions.catalog-cache.v1`）确认开始菜单/全部功能里出现 Developer Studio。

---

## 3. 根因（一句话）

生产是由 `codex/wendao-release-20260920` 的一个 **subset checkout** 部署的：该分支本身就没有开发者面（缺 `developer.html`、`SystemManager/functions.html`、`developer_platform*`、21 个 `data/sdk/*` 契约、`inc/trust-schema.asp` 等 268 个文件），却多了 `admin_api/adminGuard.asp`、`assets/js/auth-session.js` 等 59 个另一条线的东西。它**不是**当前分支的子集，两条线已经分叉（973 vs 764 文件）。

---

## 4. 给 Codex 的红线（照做，别踩）

1. **不要**把仓库版 `SystemManager/index.html` 上传/覆盖到线上。它引用 195 字节的 `assets/css/tailwind.min.css` stub（会让后台首页彻底失去样式），并依赖线上不存在的 `SystemManager/assets/js/admin-security.js` 与 `csrfToken` 后端（会让后台**无限跳转 `login.html`**）。线上安全的版本是 `deploy/developer-recovery-20260923/payload/SystemManager/index.html`。
2. **不要**把 `codex/wendao-release-20260920` 当部署源。该分支根目录的 `DEPLOYMENT-UNSUPPORTED.md` 就是为这件事放的。
3. 后台安全加固 `ce1f243` + `6381297`（强制后台请求来源 / 变更要求 CSRF 证明）**从未部署到生产**。线上 `admin_api/adminAuth.asp?action=status` 返回体里没有 `csrfToken`。要上必须**一次性整体发布**，不能拆开往线上塞。
4. 主线 `index.html` 与线上是**分叉**的（线上多了 3 个只存在于另一条线的文件 `auth-session.js` / `file-query-parser.js` / `ai-file-tools.js`，以及 16 个更新的 `?v=`）。本次只做分析、**没有改动**，要对齐得先确认哪边是权威。
5. 以后凡是改线上文件，一律走 `deploy-ftp.ps1` + `manifest.json` 的 SHA-256 双向校验，别直接覆盖。

---

## 5. 还没做的事

| 优先级 | 事项 | 状态 |
|---|---|---|
| — | 无痕窗口 UI 目测 | ⏳ 需人工 |
| — | 删除远端 4 个 `.__previous_20260923-developer-recovery` 备份 | ⏳ 验证后 |
| ✅ | `data/deploy/production-rehearsal-manifest-v1.json` 提交 | **已在 `5815521` 完成** —— 它钉住了 `production-environment-config-v1.json`、`verify_webwindows_trust_schema.sql`、`tools/build-production-rehearsal-manifest.mjs` 的当前内容，必须同批提交，否则干净检出哈希对不上 |
| P3 | 后台安全加固 `ce1f243`/`6381297` 整体上线 | ⏳ 单独一次发布 |
| P3 | 修掉主线 3 个页面引用 195B tailwind stub | ⏳ 未修 |
| P3 | 决定 `codex/wendao-release-20260920` 推还是删/改名 | ⏳ 标记已推 |
| P4 | 对齐根 `index.html` 与线上分叉线 | ⏳ 仅分析 |

### 附：`codex/wendao-release-20260920` 分支自身的部署门禁实测（2026-09-23）

该分支根目录的 `AGENTS.md` 要求部署前跑 4 道门，实测结果：

| 规则 | 结果 |
|---|---|
| 1/2 `origin/main` 为祖先 | ✅ 两个分支均通过，**无需合并** |
| 3 部署源已提交、非脏文件 | ✅ 上传的 4 个文件全部 `clean / 已提交` |
| 4 `tools/deployment-preflight.ps1` | ❌ `Deployment integrity mismatch: data/apps/system-apps.json` —— 远端新增的 13 个提交改了目录文件，但没按规则 6 重跑 `tools/update-deployment-manifest.mjs` |
| 5 上传前备份 | ✅ 远端保留 `<path>.__previous_20260923-developer-recovery` |
| 7/8 入口依赖冒烟 | ❌ `node tests/deployment-entry-dependency-smoke.mjs` 报 `ENOENT SystemManager/visitor-analytics.html` —— 该文件只存在于主线检出里**未提交**的 visitor-analytics 改动中，wendao 分支没有 |

两条 ❌ 都是**既有问题**，与本次文档改动无关，且第一条证明该分支的 manifest 已经漂移。
注意 `AGENTS.md` 规则 4 要求「分支等于 upstream 后再跑 preflight」——**先 push 再跑**才有效，
我第一次跑因为本地领先 2 个提交被直接拒掉，属顺序问题不是真失败。

> 规则 6 明确 `deploy/ftp-manifest.json` **不许手工改哈希**，必须用工具重生成。
> 但给一个标了 DO NOT DEPLOY 的分支重生成部署 manifest，等于在为它做发布准备 ——
> **本次没有执行**，交由后续决策。

---

## 6. 安全提醒

本次执行用到的 **FTP 账密已在对话记录中明文出现，请轮换**。仓库里 `tmp/deploy-studio-storage-hotfix.ps1` 等历史脚本也保留了同样的 FTP 主机/根路径信息，属既有情况，但**不要**再把凭据写进任何会被提交的文件。

---

## 7. 部署门禁移植（已完成）· 发布路径（已搁置）—— 给 Codex 的记录

> **状态**：主目标「恢复开发者工具」= **完成**。
> 部署门禁五件套移植、manifest 生成、门禁转绿 = **完成并已推送**。
> FTP 发布路径的最终选型与 preflight 跑通 = **按执行方指示搁置**，未执行。

### 7.1 本轮新增提交

| 提交 | 内容 |
|---|---|
| `5815521` | 部署门禁 5 件套**按文件摘取**（不 merge）：`AGENTS.md`、`tools/deployment-preflight.ps1`、`tools/update-deployment-manifest.mjs`、`tests/deployment-entry-dependency-smoke.mjs`、`tests/deployment-manifest-smoke.mjs`；生成 `deploy/ftp-manifest.json`（`2026.09.23.1` / previous `2026.09.21.7` / required 255 / integrity 254 / upload 31，scope `entry-dependency-alignment`）；合并 `api/function-catalog.asp`（保留主线 `WebWindowsTrustSchemaReady` + `ReleaseBindingsValid`，补生产系的 `VersionIsNewer` 数值版本比较，`SeedCatalog` 改写真实版本，**不含任何 DDL**）；成对移植 `api/mobile-version.asp` + `tests/mobile-version-contract-smoke.mjs`；移植 `cloud/browser/private-files.css`、`api/release-version.asp`；从线上同步 `sysinfo.html`/`assets/js/sysinfo.js`；修 `SystemManager/users.html` 的 `assets/styles/` 笔误与 tailwind CDN 引用；提交 rehearsal manifest 及其钉住的 3 个同伴文件 |
| `d151616` | 补交 `assets/js/vendor/html2canvas.min.js`（198,690 B，MIT）—— 被**已提交**的 `index.html:27` 引用却一直未跟踪，是三个测试在干净检出转红的根因 |
| 待提交 | `tools/normalize-line-endings.mjs`（本节配套修复工具） |

**执行方保留、未提交**：约 25 个无关脏文件、`tests/wendao-transit-smoke.mjs`（未跟踪）、`api/railway-proxy.asp`（未跟踪）、`tests/classic-asp-syntax-smoke.mjs` 的 3 处改动、`SystemManager/index.html` 的访客统计入口行。

### 7.2 干净检出才是可信的测试数字

主仓库 `84/84` 是被本地未跟踪文件和 WIP 撑起来的。用 `git worktree` 做干净检出 + 给 `node_modules` 挂 junction 后实测：

| 状态 | 结果 |
|---|---|
| 移植后（首测） | **67/83**，16 红 |
| 补交 html2canvas + 挂 node_modules 后 | **79/83**，4 红 |

剩余 4 红的归因：

| 测试 | 原因 | 归属 |
|---|---|---|
| `deployment-manifest-smoke` | 4 个上传文件（`SystemManager/feedback.html`、`assets/js/{dashboard,news,users}.js`）工作区是 CRLF，`.gitattributes` 强制检出为 LF，manifest 却钉了 CRLF 字节 | 待修，`tools/normalize-line-endings.mjs` |
| `production-deployment-rehearsal-smoke` | `.gitattributes` 规则清单**漏了它自己**，干净检出被 `core.autocrlf=true` 转成 386B（钉 370B）；`WebWindows.DeveloperPackageValidator.exe.config` 同类（171 vs 176） | 待修，同上 |
| `developer-center-docs-smoke` | `developer-samples/hello-webwindows.zip` 被 `.gitignore:29:*.zip` **永久忽略**，新克隆永远没有 | **既有缺陷**，需 `git add -f` 或改测试 |
| `visitor-analytics-smoke` | 断言 `SystemManager/index.html` 含 `visitor-analytics.html`，该行是**未提交**的 WIP | 等提交 |

> **8 个红纯粹是缺 `node_modules`**（`ajv` / `jszip` / `fake-indexeddb` / `parse5`）。验证干净检出时先挂 junction，别误判成代码问题。

### 7.3 关键事实（继续这条线之前先读）

1. **`deploy/ftp-manifest.json` 的 `integrity` 有两种互斥语义**（`tools/update-deployment-manifest.mjs`）：
   - **带 `--production`**（L79–111）：未上传文件**继承生产哈希**（含义是「生产上已是这个内容，本次不动它」），只有 `uploadFiles` 按本地字节算。
   - **不带 `--production`**（L112–143，本轮被改为）：`integrity = {}` 后**全部按本地字节**重算，`uploadFiles = --files` 给的切片。
2. **`tools/deployment-preflight.ps1` L65–76 只认第一种**：非上传文件必须与生产 manifest 逐字节一致，唯一豁免是 `reconciledFiles`（只有 `--reconcile-directory` 会生成它）。**两种语义不兼容**，实测：

   | manifest 版本 | preflight 结果 |
   |---|---|
   | `d151616`（`--production` 模式） | 4 处失败，**全是 L45**（4 个上传文件在生成后被改动）；`L62`/`L41`/`L69`/`L72` 结构性检查**全过** |
   | 2026-09-23 04:16 本地增量版（`.23.2` / scope `wendao-transit-rail` / 14 上传） | 4 处失败：**L62** 发布链断（线上仍是 `2026.09.21.7`，而本地模式拿本地旧版本号当 previous）、**L41** `api/railway-proxy.asp` 未跟踪、**L69 22 个**生产 manifest 无 integrity、**L72 163 个**本地哈希≠生产哈希且无豁免 |

   两版都过不了 —— 一版是「生成后文件又变了」，另一版是「设计契约冲突」。**提交解决不了后者。**
3. **生产 manifest 漏记它实际托管的文件**：`admin-security` 那 8 文件块、`road.html` 依赖的 `transit-*.js` 等共 **22 个**，在 `https://www.y0.hk/deploy/ftp-manifest.json` 里没有 integrity 计录。它们要么放回 `uploadFiles`（preflight L66 会跳过），要么**先补线上 manifest**。
4. **`.gitattributes` 是本仓 SHA-256 可移植性的契约，但它没写自己** —— 这是 7.2 前两红的共同根因。修法见 `tools/normalize-line-endings.mjs`：补一行自规则，再把「内容与 HEAD 逐行等价、仅换行符不同」的文件改写为 HEAD 字节（共 63 个候选，其中 22 个内容真不同的一律跳过）。

### 7.4 线上事实补充（补第 4 节红线）

6. **生产 `SystemManager/users.html` 有两个 404 样式表**：它写 `assets/styles/`，兄弟页 `datacenter.html` / `news.html` 写的是 `assets/css/` —— 这是**线上既有 bug**。仓库版已改对（`5815521`），但按红线 1 **仍不能上传**（会带出 tailwind stub 与 admin-security 依赖问题）。
7. **生产 `admin_api/adminAuth.asp?action=status` 不返回 `csrfToken`**（带 `X-WebWindows-Admin-Request: admin-auth` 时返回 `{"ok":true,"authenticated":false,"username":""}`），`inc/admin-security.asp` 线上 404。本地 `admin-security.js` 拿不到 64 位 hex token 会直接抛 `ADMIN_LOGIN_REQUIRED` —— 实测印证红线 3。
8. **`sysinfo.html` / `assets/js/sysinfo.js` 本地原本陈旧**，已从线上抓回。线上为 CRLF，按 `.gitattributes` 归一为 LF 后与 `codex/wendao-release-20260920` 的提交内容**逐字节一致**（线上就是它的 CRLF 呈现）。
9. **主线缺上游提交 `9441289`**（`fix: version Android host from production release`），它**同时**改 `api/mobile-version.asp` 与其契约测试，且两版契约互斥（旧版要求 `MapPath("../index.html")`，新版明确禁止）。**必须成对移植，别只移植单边。**
10. 生产 `assets/css/tailwind.min.css` 是 201B 桩（仓库 195B），两者都是死的 —— 生产 `SystemManager` 页面实际走 CDN Tailwind。

### 7.5 搁置项（接手前先定，本轮均**未执行**）

- **发布路径二选一未定**：
  - 走 `--production`：能过 preflight 结构性检查，但会覆盖掉 `.23.2` / `wendao-transit-rail` / 14 个上传文件那版；
  - 走本地增量：需先修 7.3-2 的 **L62**（previous 应取线上版本而非本地旧版本）与 **L69/L72**（非上传文件要么继承生产哈希，要么填 `reconciledFiles` 豁免）。
- **`tools/deployment-preflight.ps1` 本轮未跑** —— 工作区脏，必然停在 L20 `Worktree is not clean`。这是门禁**按设计工作**，不是失败。
- `tools/normalize-line-endings.mjs` **未运行**，且带前置守卫：`uploadFiles` 脏就拒绝执行，防止把未提交内容钉进 manifest。
- 浏览器 UI 目测仍待人工（浏览器未连接，缓存键 `webwindows.functions.catalog-cache.v1`）。
